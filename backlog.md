# XCSteward Backlog

## Pending

### Public alpha release gates

- [x] Prove the S0 safety contract before public alpha.
  - Severity: S0.
  - Acceptance criteria: no known path can delete or corrupt unrelated developer state, kill unrelated processes, report false success, lose artifacts for a completed or failed run, double-run the same job, strand the queue permanently, or perform broad CoreSimulator cleanup without explicit danger-level confirmation.
  - Specific checks: process termination is limited to owned process groups, simulator mutation requires an owned lease, `doctor --fix` remains narrow by default, global CoreSimulator cleanup is opt-in and scary, and cleanup/deletion commands never escape the configured state root.
  - Progress: unavailable, read-only, or malformed state roots now fail as explicit `state_root_unavailable` JSON errors before job records are queued.
  - Progress: concurrent first openers for a fresh shared state root now serialize SQLite WAL setup and schema migration with an interprocess initialization lock.
  - Progress: configured simulator IDs are now validated with a read-only `simctl list devices --json` probe before any simulator lease, boot, shutdown, erase, or xcodebuild destination is used; missing or unavailable IDs fail instead of falling back to another simulator.
  - Progress: executor startup now preflights `xcodebuild` resolution with `xcrun --find xcodebuild` before simulator validation, leasing, boot, cleanup, or xcodebuild destination mutation; unavailable Xcode fails as a runner bootstrap failure with command evidence.
  - Progress: running build/test cancellation now has focused E2E coverage proving the active owned xcodebuild is terminated, the job reports `canceled`, the simulator lease is released, and command/artifact evidence remains discoverable.
  - Progress: queued cancellation now has E2E coverage proving a queued job can be terminally canceled while another job runs, stays artifact-empty, never records process or simulator state, leaves no simulator lease, and never executes later.
  - Progress: simulator startup failures now preserve targeted command evidence and only perform targeted same-simulator cleanup; cancellation during `simctl boot` terminates the owned boot process and releases the simulator lease.
  - Progress: simulator disappearance during boot and bootstatus failure are now explicit release-matrix rows proving stable bootstrap failure, diagnostic evidence, no xcodebuild destination use, no leaked simulator lease, and no broad CoreSimulator cleanup.
  - Progress: broad CoreSimulator cleanup is now gated behind both `--fix-global` and `--dangerously-confirm-global-coresimulator-cleanup`; plain `doctor --fix` and unconfirmed `--fix-global` do not run `xcrun simctl delete unavailable`.
  - Progress: default runs now have release-matrix coverage proving build/test execution stays on job-local DerivedData, `.xctestrun`, and result-bundle paths rather than shared developer state.
  - Progress: unsafe explicit DerivedData overrides are now a release-matrix doctor gate: repo-local and external shared paths warn with manual action instead of being silently accepted or auto-fixed.
  - Progress: Xcode-managed parallel worker counts above one are now a release-matrix doctor gate: the warning is non-auto-fixable and points users to serial or one-worker execution unless a live smoke job proves clone stability.
  - Progress: cleanup now resolves symlinks before considering a terminal job directory eligible, so a job path that is textually under `jobs/` but resolves outside the configured state root is skipped and cannot delete external files or remove the job record.
  - Progress: stale worker recovery now has safety coverage proving a distinct live recorded PID that is not an executor-owned runner is not terminated, the running job and simulator lease are preserved, and only the stale worker lease is released.
  - Progress: foreign simulator leases now have focused coverage proving a new job fails before simulator mutation or build/test execution, preserves the existing lease, and leaves summary/log/run-metadata evidence.
  - Progress: malformed project profiles now fail as `runner_bootstrap_failure` before any `xcrun`/`xcodebuild` command, process record, simulator lease, or simulator mutation is created, while preserving summary/log/run-metadata evidence.
  - Progress: missing project profiles now fail the same way, as `runner_bootstrap_failure` with summary/log/run-metadata evidence before simulator or Xcode tool side effects.
  - Progress: command handlers now reject unexpected arguments and missing option values before queue mutation, so typoed flags cannot silently create jobs that fail later for the wrong reason.
  - Progress: the hardening matrix now explicitly gates both plain `doctor --fix` narrow behavior and state-root setup/database failures so destructive cleanup and queue mutation remain opt-in and preflighted.
  - Progress: project-scoped `doctor --fix` for a missing managed simulator now has release-matrix coverage proving it creates only the configured simulator, recovers stale worker leases, keeps simulator leases empty, and avoids shutdown-all, delete-unavailable, delete, and erase CoreSimulator operations.
  - Progress: protected state-root and project-profile paths are now release-matrix doctor gates: risky locations warn, remain non-auto-fixable, and provide manual remediation instead of silently treating protected paths as safe.
  - Progress: competing local runner processes and unavailable process-listing probes are now release-matrix doctor gates: both warn, remain non-auto-fixable, and provide manual next actions instead of attempting to kill unrelated developer activity.
  - Progress: process detection now tokenizes quoted and escaped command lines before classifying runner processes, so quoted option values containing words such as `test` no longer look like executable test actions.
  - Progress: broken Xcode/CoreSimulator installs are now release-matrix doctor gates for Command Line Tools selection, missing `iphonesimulator` SDKs, missing iOS runtimes, and dyld cache runtime failures; each fails as non-auto-fixable with manual remediation before simulator mutation.
  - Progress: Xcode selection drift is now release-matrix doctor coverage: `DEVELOPER_DIR` overrides warn without pretending to be auto-fixable, missing first-launch components fail with `xcodebuild -runFirstLaunch` guidance, and selected-Xcode/version mismatches fail with `xcode-select --switch` guidance.
  - Progress: CoreSimulator unavailable-device inspection failures now warn without being marked auto-fixable, require manual `simctl list devices --json` inspection before any danger-confirmed cleanup, and have matrix coverage proving device parsing variants never trigger default deletion.
  - Progress: the hardening matrix runner no longer evaluates matrix row text through `bash -c`; it validates supported `swift test --filter` argv forms, rejects shell metacharacters before execution, and has coverage proving unsafe rows do not invoke even a fake `swift`.
  - Evidence: current live-inclusive hardening matrix passed 65 rows on 2026-05-25 with zero failures and live coverage included; report: `.build/hardening-matrix/public-alpha-live-current.json`.

- [x] Prove scheduler and state invariants under contention.
  - Severity: S0.
  - Acceptance criteria: queued job claiming is atomic, only queued jobs can transition to running, every submitted job has exactly one durable job record, a job is never claimed by two workers, worker crash/restart can reconcile stale running jobs, and temporary host backpressure never permanently abandons queued work.
  - Test cases: submit 10 jobs at once, parallel `submit --wait --json`, concurrent claim attempts, worker killed while queued work exists, worker killed during a running job, backpressure followed by recovery, and stale database state recovery.
  - Progress: atomic singleton worker lease acquisition is covered by a concurrent gateway test; queued job claiming already has concurrent gateway coverage; E2E coverage now exercises 10 simultaneous submit commands and parallel `submit --wait --json` clients.
  - Progress: same-simulator contention inside XCSteward-owned concurrent jobs now waits for the active running job's simulator lease instead of failing as runner bootstrap noise; unknown/foreign simulator leases still fail fast.
  - Progress: same-simulator serialization is now an explicit hardening-matrix row proving the second concurrent job does not begin build/test work until the first job's test has ended, with no lease-conflict failure and no leaked leases.
  - Progress: unknown/foreign simulator lease behavior is now an explicit release-matrix row proving fast failure preserves the foreign lease and prevents overlapping simulator mutation.
  - Progress: worker startup now reconciles unowned stale `running` jobs whose recorded child process is gone, writes an interrupted summary, releases that job's simulator lease, and continues processing queued work; if the recorded child process is still alive, the simulator lease is preserved.
  - Progress: stale worker recovery now verifies and terminates recorded orphaned xcodebuild/test runner process groups before interrupting the job, so worker restart after build/test crashes can release the simulator lease and run later queued work.
  - Progress: temporary host backpressure is covered by fake-tool E2E tests proving queued jobs remain queued during pressure or drain, worker lease heartbeat stays alive, and dispatch resumes after the pressure/drain condition clears.
  - Progress: `submit --wait` now starts the singleton background worker and waits as a client instead of owning execution inline, so interrupting the waiting CLI does not abandon the queued or running job.
  - Progress: interrupted `submit --wait --json` clients are now explicit hardening-matrix coverage with assertions that the background worker finishes the job and leaves artifacts/run metadata discoverable.
  - Progress: canceling queued work now has explicit coverage proving the worker skips the canceled job after the active job finishes instead of claiming or executing it.
  - Progress: stale recovery now distinguishes a recoverable dead runner from a live unrelated recorded process, preserving the latter instead of marking the job interrupted or releasing its simulator lease.
  - Evidence: current live-inclusive hardening matrix passed 65 rows on 2026-05-25; scheduler rows include concurrent submit, parallel wait JSON, same-simulator serialization, foreign lease fail-fast, worker restart, stale running-job recovery, and host backpressure recovery.

- [x] Prove artifact and evidence invariants for every started run.
  - Severity: S0.
  - Acceptance criteria: every started job has a unique job directory, every attempt has unique attempt artifacts, retries never overwrite earlier attempt artifacts, the final summary points to the canonical result, and logs, metadata, `.xcresult`, JUnit, command, simulator ID, Xcode path, DerivedData path, exit status, timeout/cancel state, and result class remain discoverable after failure.
  - Test cases: retry attempt artifact uniqueness, missing `.xcresult`, corrupt `.xcresult`, JUnit generation failure, artifact parsing failure after tests finish, and cancellation after tests finish but before artifact parsing.
  - Progress: `run-metadata.json` now includes a command manifest, Xcode path, canonical summary/result/log/JUnit paths, DerivedData path, final exit code, timeout flag, and cancellation flag.
  - Progress: E2E coverage now proves the evidence trail for successful runs, build failures, test timeouts, and corrupt `.xcresult` artifact failures.
  - Progress: successful-run evidence now asserts exact job-local DerivedData, `.xctestrun`, and result-bundle paths in run metadata, artifacts JSON, and recorded xcodebuild commands.
  - Progress: build timeouts are now a distinct `build_timeout` result with command-level timeout evidence, preserved logs, and no test-phase command after the build timer fires.
  - Progress: missing `.xcresult` after a nominally successful test command is now covered as `artifact_failure`; the run preserves logs, JUnit, diagnostics, and command metadata without exposing a nonexistent result bundle.
  - Progress: JUnit generation failure after a successful test process now produces `artifact_failure` instead of false success, keeps the parsed `.xcresult` and command evidence, records the JUnit write error in logs, and does not expose a bogus JUnit path.
  - Progress: retry-enabled xcode-managed runs now have E2E assertions proving the retryable first attempt is preserved under `artifacts/attempts/`, the final `.xcresult` remains canonical, attempt metadata points only at attempt artifacts, and command metadata records both test attempts.
  - Progress: cancellation during post-test `.xcresult` summary parsing now terminates the owned `xcresulttool` probe, records the artifact command, preserves the canonical result bundle and logs, releases the simulator lease, and reports `canceled` instead of false success or artifact failure.
  - Progress: command observability now has both terminal records and live records: `commands.jsonl` remains the final command manifest, while `command-events.jsonl` records launching/started/finished/failed events and is exposed through `artifacts.commandEvents` for in-progress runs.
  - Progress: doctor project preflights are now release-matrix evidence gates for missing runnable simulator destinations, missing test plans, package resolution failures, stale `.xctestrun` files, and incompatible `xcresulttool`; each produces a non-auto-fixable diagnostic with manual remediation.
  - Progress: heavyweight doctor `.xctestrun` integrity builds now time out as bounded non-auto-fixable warnings instead of hard preflight blockers; actual `submit` runs still perform normal build/test execution and preserve artifacts if generation fails.
  - Progress: doctor `.xctestrun` integrity builds now disable Xcode indexing for the scratch probe and retry scratch cleanup, reducing leftover DerivedData churn after probe timeout.
  - Evidence: current live-inclusive hardening matrix passed 65 rows on 2026-05-25; artifact rows include isolated DerivedData, missing/corrupt `.xcresult`, JUnit failure, retry attempt preservation, and cancellation during artifact parsing. The live row proves real `.xcresult` generation.

- [x] Build and run an `XCSteward Hardening Matrix` before public alpha.
  - Severity: S0.
  - Acceptance criteria: each row has an explicit expected result stronger than "does not crash", and the matrix can be run locally with fake tools plus at least one live smoke target.
  - Initial rows: submit 10 jobs at once, submit 2 jobs with `--wait --json`, kill worker during build, kill worker during test, cancel during simulator boot, cancel during build, cancel during test, timeout during build, timeout during test, `xcodebuild` exits 65, `.xcresult` missing, `.xcresult` parse fails, retry enabled, stale running job in state DB, invalid simulator ID, read-only state root, Xcode unavailable, host capacity unavailable then recovered.
  - Progress: matrix rows for `xcodebuild` exits 65, timeout during test, and `.xcresult` parse failure now assert explicit metadata/artifact evidence, not just terminal state.
  - Progress: the stale running job row now has fake-tool E2E coverage proving worker restart recovers the stale job as `interrupted`, preserves a useful summary, releases the stale simulator lease, and runs later queued work.
  - Progress: the read-only state root row now has CLI coverage proving `submit --json` returns a stable `state_root_unavailable` error before queue mutation; file-backed and corrupt database state roots are covered too.
  - Progress: the invalid simulator ID row now has E2E coverage proving a missing configured simulator fails as `runner_bootstrap_failure`, preserves logs, leaves no simulator lease, and performs no boot/shutdown/erase or xcodebuild destination mutation.
  - Progress: the Xcode unavailable row now has E2E coverage proving `xcrun --find xcodebuild` failure is recorded in run metadata, returns `runner_bootstrap_failure`, preserves logs, leaves no simulator lease, and performs no simulator list/boot/shutdown/erase or xcodebuild destination mutation.
  - Progress: the timeout during build row now has E2E coverage proving the job fails as `build_timeout`, records the timed-out build command, preserves logs, releases the simulator lease, and never starts the test xcodebuild command.
  - Progress: the isolated-deriveddata-default row now proves default successful runs use only job-local DerivedData, `.xctestrun`, and result-bundle paths.
  - Progress: the doctor-deriveddata-override-warning row now proves explicit shared DerivedData overrides remain visible as warnings with manual remediation.
  - Progress: the doctor-xcode-managed-parallel-warning row now proves experimental higher-worker Xcode-managed parallelism is surfaced as an explicit alpha caveat with deterministic fallback guidance and a live-smoke requirement.
  - Progress: the doctor-state-root-protected-path-warning and doctor-project-protected-path-warning rows now prove doctor surfaces protected or high-risk paths for XCSteward state, repo/project/workspace paths, and explicit build-output overrides.
  - Progress: the matrix now includes doctor-no-runnable-destination, doctor-missing-test-plan, doctor-package-resolution-failure, doctor-xctestrun-current-build-required, and doctor-modern-xcresulttool-required rows to prove common project preflight failures are specific and actionable.
  - Progress: the matrix now includes doctor-xctestrun-integrity-timeout-warning so slow real-project integrity probes remain diagnosable without blocking later job evidence collection.
  - Progress: the matrix now includes doctor-competing-runner-process-warning and doctor-process-listing-unavailable rows proving local process contention is diagnosable without destructive auto-fix behavior.
  - Progress: the matrix now includes doctor-command-line-tools-selected, doctor-iphonesimulator-sdk-missing, doctor-ios-runtime-missing, and doctor-runtime-dyld-cache-error rows to keep Xcode/CoreSimulator environment failures diagnosable before alpha.
  - Progress: the matrix now includes doctor-developer-dir-override-warning, doctor-first-launch-components-missing, and doctor-xcode-cli-version-mismatch rows for Xcode selection drift and first-launch setup failures.
  - Progress: the matrix now includes doctor-unavailable-runtime-warning, doctor-runtime-availability-parser-variants, doctor-coresim-json-health-failure, doctor-unavailable-device-inspection-failure, and doctor-unavailable-device-parser-variants rows to prove CoreSimulator enumeration/parsing stays conservative and non-destructive.
  - Progress: the `.xcresult` missing row now has fake-tool E2E coverage proving a successful test process without a result bundle fails as `artifact_failure`, records the successful test command, releases the simulator lease, and keeps artifact metadata discoverable.
  - Progress: the cancel during build and cancel during test rows now have fake-tool E2E coverage proving `canceled` terminal state, SIGTERM observation by the owned xcodebuild process, command exit evidence, preserved logs/artifacts, and lease release.
  - Progress: the kill worker during build and kill worker during test rows now have fake-tool E2E coverage proving worker restart terminates verified orphan xcodebuild processes, marks the abandoned job `interrupted`, releases the simulator lease, and runs the next same-simulator job.
  - Progress: the simulator-disappears-during-boot and simulator-bootstatus-failure rows now prove simulator bootstrap failures produce targeted evidence and avoid xcodebuild or broad CoreSimulator cleanup.
  - Progress: the cancel during simulator boot row now has fake-tool E2E coverage proving `simctl boot` receives SIGTERM, the job reports `canceled`, the simulator lease is released, and no xcodebuild command starts.
  - Progress: the cancel-queued-job row now proves queued cancellation is terminal, artifact-empty, process-free, simulator-free, and not executed later by the worker.
  - Progress: the submit-wait-client-interrupted row now proves an interrupted waiting client does not kill the background worker or lose the completed job's artifacts.
  - Progress: the same-simulator-lease-serialization row now proves same-simulator concurrent jobs are serialized by the lease instead of overlapping simulator mutation or failing with lease contention.
  - Progress: the foreign-simulator-lease-fails-fast row now proves unknown lease contention becomes a diagnosable bootstrap failure before simulator mutation or build/test execution.
  - Progress: the junit-generation-failure row now proves JUnit write errors after successful tests become artifact failures with parsed counts, `.xcresult`, logs, and no bogus JUnit artifact.
  - Progress: the host capacity unavailable then recovered row has fake-tool E2E coverage for sampled memory-pressure backpressure and infrastructure-failure drain windows; jobs stay queued while capacity is zero/constrained and later complete without manual intervention.
  - Progress: the retry enabled row now asserts canonical final result-bundle preservation separately from first-attempt artifacts and verifies both failed-first and successful-retry test commands are recorded.
  - Progress: `docs/hardening-matrix.md` now provides a runnable row-by-row release-gate manifest with explicit expected results, fake-tool `swift test --filter` commands, the danger-confirmed global cleanup row, and the opt-in live xcode-managed smoke command; `HardeningMatrixTests` fails if required rows or commands disappear.
  - Progress: `scripts/run-hardening-matrix.sh` now runs the fake-tool matrix rows sequentially, supports listing or selecting a single row, skips the live simulator smoke by default, and requires explicit `--include-live` plus `XCSTEWARD_LIVE_SIMULATOR_ID` for live execution.
  - Progress: hardening matrix runs now leave a JSON report with overall status, timing, row IDs, commands, exit codes, durations, and live-skip state; failure paths write the report before exiting nonzero.
  - Progress: the hardening matrix runner now supports `--continue-on-failure` so release checks can inventory all selected failing rows in one JSON report while still exiting nonzero; its manifest parser also preserves the final row when a Markdown file has no trailing newline.
  - Progress: the matrix runner now executes selected rows with argv-based `swift` invocation after command validation, rejects unsafe shell syntax before writing a report or invoking tools, and has explicit fake-tool coverage for the opt-in live smoke row.
  - Progress: the matrix manifest and runner now have anti-drift coverage proving row IDs are unique and `run-hardening-matrix.sh --list` exposes exactly the documented fake rows in release-gate order.
  - Progress: state root file-backed and corrupt database path failures are now explicit matrix rows alongside the read-only state root row, each requiring stable `state_root_unavailable` JSON before queue mutation.
  - Progress: plain `doctor --fix` refusing to delete unavailable CoreSimulator devices is now an explicit matrix row separate from the dangerous global cleanup confirmation row.
  - Progress: the matrix now includes a doctor-managed-simulator-fix-scoped row proving project-level simulator repair remains narrow while stale worker lease recovery stays explicit and inspectable.
  - Progress: the matrix now includes a cleanup state-root containment row that proves symlinked job directories resolving outside `jobs/` are not selected or deleted.
  - Progress: the matrix now includes a process-monitoring-error row proving ambiguous `waitpid`/process monitor failures become failed jobs with evidence instead of false success or stranded state.
  - Progress: the matrix now includes a foreign-live-process-preserved row proving stale recovery does not terminate unrelated live recorded processes or release their simulator lease.
  - Progress: the matrix now includes a malformed-profile-fails-before-mutation row proving bad profile configuration fails with evidence before `xcrun`, `xcodebuild`, or simulator mutation starts.
  - Progress: the matrix now includes a doctor-iphonesimulator-sdk-runtime-mismatch row proving selected Xcode SDK/runtime mismatches are diagnosed generically before live runs.
  - Evidence: current live-inclusive hardening matrix passed 65 rows on 2026-05-25 with zero failures and live coverage included; report: `.build/hardening-matrix/public-alpha-live-current.json`.

- [x] Audit recovery behavior so ugly local failures become boring.
  - Severity: S0/S1 depending on failure mode.
  - Acceptance criteria: each expected failure yields a stable result class, preserves useful logs/artifacts, keeps the queue healthy, and explains next action without requiring manual database surgery.
  - Scenarios: `simctl` failure, simulator boot failure, selected simulator disappears, CLI waiting client interrupted, stale running jobs, state root missing/read-only/partially corrupt, timeout during build/test, cancellation during boot/build/test, and ambiguous process-monitoring errors.
  - Progress: stale running jobs without a live worker owner now become a stable `interrupted` result with default artifact paths and an explicit summary line; jobs with a still-live recorded child process are left running and keep their simulator lease to avoid unsafe simulator mutation.
  - Progress: state root setup and database-open failures now produce a stable `state_root_unavailable` error code and clear path-bearing message instead of leaking generic Foundation/SQLite failures.
  - Progress: the release matrix now requires the state-root file and corrupt database-path variants to stay diagnosable and avoid queue mutation, not just the read-only-directory variant.
  - Progress: missing or unavailable configured simulators now produce a stable `runner_bootstrap_failure` with a clear no-fallback/no-mutation summary and preserved logs before simulator state can be mutated.
  - Progress: missing `xcodebuild` now produces a stable `runner_bootstrap_failure` with the `xcrun --find xcodebuild` exit recorded before any simulator state can be mutated.
  - Progress: build timeouts now produce a stable `build_timeout` result and stop before test execution while leaving run metadata that identifies the timed-out build command and configured timeout.
  - Progress: missing `.xcresult` now produces a stable `artifact_failure` instead of false success, with logs, JUnit, diagnostics, and command evidence left behind for diagnosis.
  - Progress: post-test JUnit generation failure now produces a stable `artifact_failure` with logs, parsed counts, `.xcresult`, command metadata, and simulator diagnostics preserved.
  - Progress: cancellation during build and test now produces stable `canceled` results with command-level exit evidence, useful logs/artifacts, and no stranded simulator lease.
  - Progress: cancellation before a job starts now remains boring and side-effect free: the job reports `canceled`, artifacts remain empty, no runner process or simulator ID is recorded, and the queue continues with other work.
  - Progress: worker death during build and test now recovers as `interrupted`, writes a recovery log entry, terminates only verified recorded runner process groups, releases the simulator lease, and keeps the queue moving.
  - Progress: simulator boot failure, bootstatus failure, and a simulator disappearing after validation now produce stable `runner_bootstrap_failure` results with logs, diagnostics, command metadata, no xcodebuild execution, and no stranded simulator lease.
  - Progress: simulator bootstatus and disappearing-device failures now explicitly assert no broad `simctl delete unavailable`, no destination handoff to xcodebuild, and preserved command evidence in the public-alpha matrix.
  - Progress: host-capacity backpressure writes `host-health.json` with explicit reasons, preserves queued jobs without marking them failed, and resumes dispatch once memory pressure or infrastructure drain clears.
  - Progress: an interrupted `submit --wait` client no longer stops the worker; fake-tool E2E coverage proves the waiting process can receive SIGINT while the background worker finishes the job and releases its lease.
  - Progress: interrupted wait-client recovery now also asserts the finished job keeps artifact and run-metadata evidence discoverable after the original waiting process exits.
  - Progress: xcode-managed test outcome classification now treats bootstrap/configuration phrases as `runner_bootstrap_failure` only before test execution starts, so test output that mentions those phrases after `Testing started` remains a test failure or test timeout instead of being retried/misreported as pre-test infrastructure failure.
  - Progress: cancellation after the test process exits but before artifact parsing finishes now becomes a stable `canceled` result with the `.xcresult`, logs, artifact-probe command evidence, and no stranded simulator lease.
  - Progress: ambiguous process-monitoring errors during build now have worker-level coverage proving the job is recorded as failed `internal_error`, logs and command metadata preserve the monitor error, process tracking is cleared, simulator leases are released, and tests are not started.
  - Progress: stale recovery of a running job with an unrelated live recorded process now remains conservative: the job stays running, the simulator lease stays held, the stale worker lease is cleared, and no signal is sent to that process.
  - Progress: foreign simulator lease contention now remains boring and diagnosable: the new job fails as `runner_bootstrap_failure`, records logs and metadata, starts no build/test command, and leaves the existing lease intact.
  - Progress: malformed project profile recovery is now stable and diagnosable: the job reaches a terminal `runner_bootstrap_failure`, the profile error is written to the summary, combined log, and run metadata, and no simulator/tool side effects occur.
  - Progress: missing profile recovery is now stable and diagnosable too: `submit --wait --json` returns a terminal `runner_bootstrap_failure` summary with persisted evidence instead of an ambiguous internal error.
  - Progress: doctor project preflight failures for destinations, test plans, Swift package resolution, stale `.xctestrun` output, and unsupported `xcresulttool` now remain boring: the CLI fails clearly, marks checks non-auto-fixable, and leaves manual next actions.
  - Progress: doctor process-contention recovery now remains boring: competing `xcodebuild` activity or a failed `ps` probe yields a warning with manual remediation instead of an attempted cleanup or ambiguous pass.
  - Progress: doctor Xcode/CoreSimulator environment failures now remain boring: CLT selection, missing simulator SDKs, missing iOS runtimes, and dyld cache problems return clear failed checks with non-auto-fixable manual actions.
  - Progress: doctor Xcode selection drift now remains boring: `DEVELOPER_DIR`, first-launch, and selected-version mismatches produce explicit warn/fail checks with manual remediation and no unsafe auto-fix.
  - Progress: doctor CoreSimulator enumeration failures now remain boring: slow global JSON enumeration fails with manual health guidance, unavailable-device list failures warn without auto-fix, and runtime/device availability parser variants are covered without default cleanup.
  - Progress: doctor now has a generic `global.iphonesimulator_runtime_compatible` check, so selected Xcode SDK/runtime mismatches produce a direct non-auto-fixable diagnostic instead of only surfacing through project destination failure.
  - Progress: doctor `xcodebuild -showdestinations` parsing now uses one available-destination extractor for both concrete and placeholder checks and preserves comma-containing destination values, reducing false placeholder/concrete classification from text-output variants.
  - Evidence: current live-inclusive hardening matrix passed 65 rows on 2026-05-25; recovery rows cover simulator, worker, state-root, process-monitoring, artifact, cancellation, timeout, Xcode/CoreSimulator, profile-failure, and live Xcode-managed smoke paths.

- [x] Complete live dogfood gate for public alpha.
  - Severity: S0 release gate.
  - Acceptance criteria: at least 3 real iOS projects, at least 2 different project shapes, at least 100 real local jobs, at least 20 intentionally induced failures, at least 10 cancellation/timeout/crash scenarios, zero S0 incidents, and every failure leaves useful logs and metadata without manual database surgery.
  - Include one live `XCSTEWARD_RUN_LIVE_XCODE_MANAGED_SMOKE=1` pass against a real simulator before tagging alpha.
  - Complete: real local job volume is at target; intentionally induced failures and cancellation/timeout/crash scenarios are also at target.
  - Progress: the live smoke default now targets `Examples/DemoApp/DemoApp.xcodeproj`, and `submit --wait` supports `--wait-timeout` so live gates can outlast normal first-run simulator startup without client-side false timeouts.
  - Progress: live `doctor` now rejects Xcode's placeholder "Any iOS Simulator Device" destination as non-runnable and skips the heavier `.xctestrun` integrity build, so the current live-smoke blocker is reported quickly as a destination/platform issue instead of hanging in `build-for-testing`.
  - Progress: installing the iOS 26.5 simulator runtime resolved the local Xcode 26.5/CoreSimulator runtime mismatch; doctor now passes `global.iphonesimulator_runtime_compatible` on the intended support host.
  - Progress: direct live xcode-managed smoke passed on 2026-05-22 against `Examples/DemoApp` and iPhone 17 Pro iOS 26.5 simulator `CBB8584A-F555-4C67-B700-6F858ACAE0E8`.
  - Progress: full live-inclusive hardening matrix passed on 2026-05-25, 65 rows, zero failures, report: `.build/hardening-matrix/public-alpha-live-current.json`.
  - Progress: dogfood state root `/private/tmp/xcsteward-dogfood-2026-05-22` now has 97 terminal real XCSteward jobs: seventy-three DemoApp-family successes, three DemoApp-family cancellations, six DemoApp-family timeout failures, thirteen induced DemoApp-family profile/config failures, one Publiqueitor test timeout, and one Lernit test failure, all with preserved evidence.
  - Progress: Publiqueitor and Lernit now count as real-project terminal job coverage. Publiqueitor reached `test_timeout` after a successful build and 900s test timeout; Lernit reached `test_failure` after a successful build and parsed `.xcresult` summary.
  - Progress: doctor `.xctestrun` integrity timeouts are now bounded warnings rather than hard failures, which should let dogfood continue to terminal submit jobs for real projects while preserving manual full-preflight guidance.
  - Progress: the live Publiqueitor doctor retry confirmed the timeout is now a warning; it also exposed scratch cleanup churn, now addressed generically by disabling indexing for the probe and retrying scratch cleanup. A second retry confirmed the cleanup warning is gone.
  - Progress: project-scoped `doctor --fix` now repairs managed simulator profiles that use display names by resolving the configured device type and runtime to CoreSimulator identifiers before `simctl create`; the live Publiqueitor managed simulator was created on iOS 26.5.
  - Progress: post-run doctor checks after the Publiqueitor timeout and Lernit test failure reported no stale worker lease and no simulator leases, so the queue remained healthy without database surgery.
  - Progress: live `xcodebuild test-without-building` observability now has a durable in-progress trail: command start markers are written to the phase and combined logs before the child process exits, and `artifacts.commandEvents` points to a machine-readable `command-events.jsonl` stream with launching/started/finished/failed events.
  - Progress: the parallel first-doctor finding is fixed at the state-store layer: fresh state-root initialization now uses an interprocess SQLite setup lock, and the hardening matrix includes a concurrent-state-root-initialization row.
  - Progress: latest dogfood batches added live cancel-during-build and cancel-during-test runs, build/test timeout runs, invalid-simulator, missing-scheme, missing-test-plan, missing-project, missing-repo-root, and wrong-typed-profile induced failures.
  - Progress: dogfood exposed missing repo roots as a misleading `internal_error` caused by launching `xcrun` from a nonexistent working directory. Executor profile-root validation now fails this case before tool launch as `runner_bootstrap_failure`, covered by `ProfileFailureRecoveryTests`.
  - Progress: wrong-typed known profile values now fail with clear `invalidConfiguration` messages instead of silently using defaults; the live `demo-malformed-profile` rerun failed as `runner_bootstrap_failure` before tool launch.
  - Progress: the transient DemoApp destination enumeration regression reproduced once after a clean volume batch: `simctl` showed the configured simulator booted and an immediate raw `xcodebuild -showdestinations` retry listed concrete iOS 26.5 simulators. Doctor now retries one transient placeholder-only destination response before failing, while persistent placeholder-only output still fails clearly and skips heavier `.xctestrun` integrity work.
  - Progress: latest dogfood batch added three clean targeted DemoApp runs, invalid-simulator and missing-project induced failures, plus build-timeout and test-timeout failures. Post-batch doctor again reported no stale worker lease, no simulator leases, no runner contention, and a responsive configured simulator bootstatus.
  - Progress: follow-up dogfood batch added two clean targeted DemoApp runs, a missing-scheme induced failure, and build/test timeout repeats. Induced-failure coverage is now 22/20 and cancellation/timeout/crash coverage is now 10/10; post-batch doctor again reported no stale worker lease, no simulator leases, no runner contention, and a responsive configured simulator bootstatus.
  - Progress: latest volume-only dogfood batch added five clean targeted DemoApp runs. Post-batch doctor again reported no stale worker lease, no simulator leases, no runner contention, and a responsive configured simulator bootstatus; disk pressure remains the limiting host warning at 2.1% free.
  - Progress: latest low-disk volume dogfood batch added three clean targeted DemoApp runs. Post-batch doctor again reported no stale worker lease, no simulator leases, no runner contention, and a responsive configured simulator bootstatus; disk pressure remains the limiting host warning at 2.2% free.
  - Progress: 2026-05-23 volume dogfood batch added five clean targeted DemoApp runs. Post-batch doctor again reported no stale worker lease, no simulator leases, no runner contention, and a responsive configured simulator bootstatus; disk pressure remains the limiting host warning at 2.4% free.
  - Progress: follow-up 2026-05-23 volume dogfood batch added five clean targeted DemoApp runs. Post-batch doctor again reported no stale worker lease, no simulator leases, no runner contention, and a responsive configured simulator bootstatus; disk pressure remains the limiting host warning at 2.4% free.
  - Progress: latest 2026-05-23 volume dogfood batch added five clean targeted DemoApp runs. Post-batch diagnosis found one transient placeholder-only `xcodebuild -showdestinations` response; manual retry and rebuilt doctor both passed, queue and simulator lease checks stayed healthy, and disk pressure remains the limiting host warning at 2.3% free.
  - Progress: follow-up low-disk 2026-05-23 volume dogfood batch added five clean targeted DemoApp runs. Post-batch doctor reported no stale worker lease, no simulator leases, no runner contention, concrete runnable destinations, and a responsive configured simulator bootstatus; disk pressure remains the limiting host warning at 2.2% free.
  - Progress: first constrained 2026-05-23 volume dogfood batch added three clean targeted DemoApp runs. Post-batch doctor reported no stale worker lease, no simulator leases, no runner contention, concrete runnable destinations, and a responsive configured simulator bootstatus; disk pressure remains the limiting host warning at 2.1% free.
  - Progress: follow-up constrained 2026-05-23 volume dogfood batch added three clean targeted DemoApp runs. Post-batch doctor reported no stale worker lease, no simulator leases, no runner contention, concrete runnable destinations, and a responsive configured simulator bootstatus; disk pressure remains the limiting host warning at 2.0% free.
  - Progress: third constrained 2026-05-23 volume dogfood batch added three clean targeted DemoApp runs. Post-batch doctor reported no stale worker lease, no simulator leases, no runner contention, concrete runnable destinations, and a responsive configured simulator bootstatus; disk pressure remains the limiting host warning at 2.0% free with just under 20 GB available.
  - Progress: fourth constrained 2026-05-23 volume dogfood batch added three clean targeted DemoApp runs. Post-batch doctor reported no stale worker lease, no simulator leases, no runner contention, concrete runnable destinations, and a responsive configured simulator bootstatus; disk pressure remains the limiting host warning at 2.0% free with 19.45 GB available.
  - Progress: latest low-disk 2026-05-23 volume dogfood batch added two clean targeted DemoApp runs. Post-batch doctor reported no stale worker lease, no simulator leases, no runner contention, concrete runnable destinations, and a responsive configured simulator bootstatus; disk pressure remains the limiting host warning at 1.9% free with 18.65 GB available.
  - Progress: single-job low-disk 2026-05-23 volume dogfood increment added one clean targeted DemoApp run. Post-batch doctor reported no stale worker lease, no simulator leases, no runner contention, concrete runnable destinations, and a responsive configured simulator bootstatus; disk pressure remains the limiting host warning at 1.9% free with 18.53 GB available.
  - Progress: low-disk dogfood diagnosis fixed a cleanup-reporting gap: doctor disk warnings now point to `cleanup --dry-run` with the same state root before preserved evidence is deleted, and cleanup size-budget reports use allocated disk bytes rather than logical file size. The live dogfood state root now reports about 8.9 GB of managed allocated bytes and no candidates under a 30 GB dry-run budget.
  - Progress: post-cleanup 2026-05-23 volume dogfood batch added three clean targeted DemoApp runs. Post-batch doctor reported no stale worker lease, no simulator leases, no runner contention, concrete runnable destinations, and a responsive configured simulator bootstatus; disk pressure remains a known host warning at 3.0% free with about 30 GB available.
  - Progress: follow-up post-cleanup 2026-05-23 volume dogfood batch added five clean targeted DemoApp runs. Post-batch doctor reported no stale worker lease, no simulator leases, no runner contention, concrete runnable destinations, and a responsive configured simulator bootstatus; disk pressure remains a known host warning at 2.8% free with 28.26 GB available.
  - Progress: latest post-cleanup 2026-05-23 volume dogfood batch added five clean targeted DemoApp runs. Post-batch doctor reported no stale worker lease, no simulator leases, no runner contention, concrete runnable destinations, and a responsive configured simulator bootstatus; disk pressure remains a known host warning at 3.0% free with 29.71 GB available.
  - Progress: closing 2026-05-23 volume dogfood batches added ten clean targeted DemoApp runs, bringing real local jobs to 100/100. Midpoint and final doctor runs reported no stale worker lease, no simulator leases, no runner contention, concrete runnable destinations, and a responsive configured simulator bootstatus; disk pressure remains a known host warning at 2.8% free with 28.13 GB available.
  - Progress: 2026-05-24 follow-up live batch added two more clean targeted DemoApp runs, bringing real local jobs to 102. Preflight and post-batch doctor both reported no stale worker lease, no simulator leases, no runner contention, concrete runnable destinations, and a responsive configured simulator bootstatus; the remaining warnings were the known unavailable-device cleanup notice, low disk pressure at 2.7% free with 27.09 GB available, and experimental xcode-managed parallelism.
  - Progress: 2026-05-24 non-DemoApp follow-up added a targeted Publiqueitor success through the real `Stable` test plan and managed-simulator path, bringing real local jobs to 103. Preflight and post-run doctor both reported no stale worker lease, no simulator leases, and a healthy managed-simulator check; the remaining warnings were the known unavailable-device cleanup notice, bounded `.xctestrun` integrity timeout warning, and disk pressure tightened to 2.2% free with 21.42 GB available.
  - Progress: 2026-05-24 `batch-bg-remover` dogfood added a fourth real iOS project and two terminal `bgremover-app` successes, bringing real local jobs to 105. Preflight and final doctor passed all project checks and reported no stale worker lease, no simulator leases, no runner contention, concrete runnable destinations, and successful `.xctestrun` generation; both live jobs preserved `.xcresult`, JUnit, command events, logs, and job-local DerivedData, and the derived 150s artifact probe timeout avoided the previous false `artifact_failure` path. Remaining warnings were known unavailable devices and disk pressure at 2.0% free.
  - Progress: 2026-05-24 `Recursia` dogfood added a fifth real iOS project preflight. Doctor passed state, Xcode/CoreSimulator, scheme, runnable destination, package resolution, worker lease, simulator lease, and runner-contention checks, then failed `project.xctestrun_integrity`; a manual equivalent `xcodebuild build-for-testing` confirmed the selected scheme currently has project compile errors in `Tests/KanbanUIKitTests/KanbanDragDropCoordinatorTests.swift`, so no XCSteward submit was run. Post-diagnostic doctor still reported no stale worker lease and no simulator leases.
  - Progress: 2026-05-24 `CucumberSwift` dogfood added a sixth real iOS project and a package-backed Xcode library/test shape, bringing terminal real local jobs to 107 when including the concurrently observed `batch-bg-remover` success. Broad `CucumberSwift` preflight failed safely at `.xctestrun` generation with manual `xcodebuild` confirming project-side `Bundle.module` compile errors; focused `CucumberSwiftDSLConsumerTests` passed doctor and job `77cb36dd-aeeb-43be-bb69-e1db853e3dd5` succeeded with 18 tests, preserved artifacts, and no artifact-probe warnings. A transient CoreSimulator enumeration timeout during another running job cleared without manual intervention, and final doctor reported no stale worker lease, no simulator leases, no runner contention, and healthy project checks.
  - Progress: 2026-05-25 `lernit_flutter.claude` dogfood added a true Flutter/CocoaPods `.xcworkspace` project shape, bringing real projects to 7, terminal-project coverage to 6, project shapes to 4, and real local jobs to 108. Doctor passed scheduler, Xcode/CoreSimulator, scheme, concrete simulator destination, package, and lease checks, then failed `.xctestrun` integrity with retained evidence for a project-side CocoaPods sandbox mismatch. Submit job `189319eb-921f-412f-9253-a5b5b6f59a76` failed as `build_failure` with xcodebuild exit 65, job-local DerivedData, logs, command events, and run metadata preserved; post-run doctor again reported no stale worker lease, no simulator leases, no runner contention, and responsive bootstatus.
  - Progress: 2026-05-25 `tel2whats` dogfood added a React Native/Expo/CocoaPods `.xcworkspace` app with share extension, bringing real projects to 8, terminal-project coverage to 7, project shapes to 5, and real local jobs to 110 when including the concurrently observed successful `batch-bg-remover` run. Raw Xcode output initially exposed only a placeholder simulator destination, doctor recovered concrete destinations on retry, warned about the concurrent runner, then failed `.xctestrun` integrity with retained evidence for a missing Pods xcconfig. Submit job `3fddcd31-7a3d-42f6-9da6-2c30fb49fe1d` failed as `build_failure` with xcodebuild exit 65, job-local DerivedData, logs, command events, and run metadata preserved; post-run doctor reported no stale worker lease, no simulator leases, no runner contention, and responsive bootstatus.
  - Progress: 2026-05-25 `lernit-expo` dogfood added another Expo/CocoaPods workspace with concrete raw destinations and no obvious test target, bringing real projects to 9, terminal-project coverage to 8, and real local jobs to 111. Doctor passed scheduler, Xcode/CoreSimulator, scheme, runnable destination, package, and lease checks, then warned on bounded `.xctestrun` preflight timeout with retained evidence. Submit job `6fb4d755-cc70-4dfa-838c-e12f76919f83` failed as `build_failure` after 241.2s with a project-side `expo-video` Swift compile error, while preserving job-local DerivedData, logs, command events, run metadata, and summary; post-run doctor again reported no stale worker lease, no simulator leases, no runner contention, and responsive bootstatus.
  - Progress: 2026-05-25 `backin15` dogfood added a Swift `.xcodeproj` app with unit tests, Swift package dependencies, and a Live Activity extension target, bringing real projects to 10, terminal-project coverage to 9, project shapes to 6, and real local jobs to 112. Doctor passed scheduler, Xcode/CoreSimulator, scheme, runnable destination, package, and lease checks, then failed `.xctestrun` integrity with retained evidence for stale test-code compile errors. Submit job `d6944b32-19b9-4e6d-99f8-feb0ced40539` failed as `build_failure` after 76.7s with xcodebuild exit 65, job-local DerivedData, logs, command events, run metadata, and summary preserved; post-run doctor reported no stale worker lease, no simulator leases, no runner contention, and responsive bootstatus.
  - Progress: 2026-05-25 `swift-cyment-arrow` UIKit demo dogfood added a package-backed UIKit snapshot-test scheme, bringing real projects to 11, terminal-project coverage to 10, project shapes to 7, and real local jobs to 113. Raw Xcode destination output emitted `IDERunDestination` platform warnings while still listing concrete simulators; doctor correctly passed runnable-destination and `.xctestrun` checks. Submit job `0efe7317-9482-4938-9cd8-fb65fcc0d0e4` succeeded after 33.5s with 3 tests, valid `.xcresult`, JUnit, job-local DerivedData, logs, command events, run metadata, and no artifact probe warnings; post-run doctor again reported no stale worker lease, no simulator leases, no runner contention, and responsive bootstatus.
  - Progress: 2026-05-25 `rickytuntun` dogfood added a Rust/cargo-backed iOS `.xcodeproj` app with no XCTest bundle, bringing real projects to 12, terminal-project coverage to 11, project shapes to 8, and real local jobs to 114. Doctor passed scheduler, Xcode/CoreSimulator, scheme, runnable destination, package, and lease checks, then warned on bounded `.xctestrun` preflight timeout with retained evidence. Submit job `84db4680-abdf-40bc-8ae4-47da0d82f2fc` completed a long `build-for-testing` after about 449s, then failed `test-without-building` as `runner_bootstrap_failure` because Xcode reported no test bundles available; XCSteward preserved `.xcresult`, JUnit, sim diagnostics, job-local DerivedData, logs, command events, run metadata, and summary. Post-run doctor reported no stale worker lease and no simulator leases, while retaining evidence for a separate Rust compile failure during follow-up preflight.
  - Progress: 2026-05-25 `flutter_rough` dogfood added a Flutter example iOS workspace with no XCTest bundle, bringing real projects to 13, terminal-project coverage to 12, project shapes to 9, and real local jobs to 115. Raw Xcode destination output initially showed only placeholder simulator destinations, but doctor recovered a runnable destination and passed `.xctestrun` generation. Submit job `9fefa0b6-350a-4340-9720-45acb3515b8c` completed `build-for-testing`, then failed `test-without-building` as `runner_bootstrap_failure` because Xcode reported no test bundles available; XCSteward preserved `.xcresult`, JUnit, sim diagnostics, job-local DerivedData, logs, command events, run metadata, and summary. Post-run doctor reported no stale worker lease and no simulator leases.
  - Progress: 2026-05-25 `auguri-native` dogfood added a SwiftPM-heavy Xcode `.xcodeproj` app with Firebase/gRPC binary dependencies and unit/UI test targets, bringing real projects to 14, terminal-project coverage to 13, project shapes to 10, and real local jobs to 116. Dogfood exposed a doctor parser gap where warning-prefixed `xcodebuild -list -json` output was misclassified as `project.scheme: Scheme is missing`; the parser now extracts the JSON object from warning-prefixed output and focused coverage proves `Feliche` is detected. After the fix, doctor passed scheme, runnable destination, package, queue, and lease checks, then failed `.xctestrun` integrity with retained compile-error evidence. Submit job `651812c4-c05a-4dac-929a-bd8f8cbca431` failed as `build_failure` after 83.2s during SwiftPM package resolution because Xcode could not remove an extracted `grpc-binary` artifact in job-local DerivedData; XCSteward preserved logs, command events, run metadata, summary, and healthy lease state, and post-run doctor reported no stale worker lease or simulator leases.
  - Progress: 2026-05-25 `lernit-mvp` dogfood added another real CocoaPods `.xcworkspace` app, bringing real projects to 15, terminal-project coverage to 14, and real local jobs to 117. Doctor passed scheme, runnable destination, package, queue, and lease checks, then failed `.xctestrun` integrity with retained evidence for a missing `Pods-Lernit.debug.xcconfig`. Submit job `7f85e469-eb86-4f4c-9d00-4762ba11cf7c` failed as `build_failure` after 21.1s with xcodebuild exit 65 and the same project-side CocoaPods configuration error; XCSteward preserved logs, command events, run metadata, summary, and healthy lease state, and post-run doctor reported no stale worker lease, no simulator leases, no runner contention, and responsive bootstatus.
  - Progress: 2026-05-25 `Recursia_mutated` dogfood added another package-backed Xcode iOS app/test profile, bringing real projects to 16, terminal-project coverage to 15, and real local jobs to 118. Doctor passed all global and project checks, including `.xctestrun` generation. Targeted submit job `1ea2b0bd-cc97-40de-aa68-002f2af5c457` for `KanbanUIKitTests/KanbanUIKitTests/testInit` succeeded after 88.6s with 1 test, valid `.xcresult`, JUnit, job-local DerivedData, logs, command events, run metadata, no artifact probe warnings, and post-run doctor reported no stale worker lease, no simulator leases, no runner contention, and responsive bootstatus.
  - Progress: 2026-05-25 live concurrent-request dogfood launched two simultaneous `submit --wait --json` clients for `demo-app` against the same simulator, bringing real local jobs to 120. Both clients returned independent terminal success JSON: job `35cb2f29-cfc1-478b-9adb-afb885c9e386` and job `0cb166e6-17aa-4c58-92a0-40ec90365486`, each with 1 test, valid `.xcresult`, JUnit, job-local DerivedData, command events, run metadata, and no artifact probe warnings. Command-event timestamps show live same-simulator serialization: first job test finished at `1779745918.438827`, while the second job's first tool command started at `1779745922.528161` and build started at `1779745925.1671438`. Post-run doctor reported no stale worker lease and no simulator leases; only unrelated local runner contention and the known xcode-managed parallelism warning remained.
  - Progress: 2026-05-25 live progress-stream dogfood added one clean targeted DemoApp run, bringing real local jobs to 121. `doctor --json --progress` emitted check-level progress events before final stdout JSON, and `submit --wait --json --progress` emitted queued, worker-ready, running heartbeat, summary-visible, and terminal events before final success JSON. Job `354741a7-c611-41b8-a491-4e866a596e49` succeeded with 1 test, valid `.xcresult`, JUnit, job-local DerivedData, command events, run metadata, and no stale worker or simulator leases after completion. The only new minor finding was a non-fatal `xcodebuild -help` evidence-probe timeout recorded in `probe_warnings`.
  - Progress: 2026-05-25 failure-path progress-stream dogfood added one terminal `lernit-mvp-workspace` build failure, bringing real local jobs to 122. `doctor --json --progress` identified `project.xctestrun_integrity` as the failing check and final JSON included retained evidence plus the missing Pods xcconfig excerpt. Submit job `26894e6c-758f-43b3-807a-889d5b747286` emitted queued, worker-ready, running, summary-visible, and terminal events before final `build_failure` JSON, preserved job-local DerivedData, build/combined logs, command events, run metadata, summary, and xcodebuild help evidence, and left no stale worker or simulator leases. `probe_warnings` was empty.
  - Progress: 2026-05-25 `xcodebuild -help` probe regression dogfood added one clean targeted DemoApp run, bringing real local jobs to 123. Preflight and post-run `doctor --json --progress` reported no stale worker lease, no simulator leases, no runner contention, healthy CoreSimulator enumeration, and acceptable disk pressure. Submit job `acaf98ff-def1-4eca-8267-50dd734a6b7a` succeeded after 30.7s with valid `.xcresult`, JUnit, command events, run metadata, captured `xcodebuild-help.txt`, derived 150s `xcresulttool` summary timeout, and empty `probe_warnings`.
  - Progress: 2026-05-25 live suite-health dogfood added one xcode-managed live smoke job, bringing real local jobs to 124. `scripts/run-test-suite.sh --tier live` produced `.build/test-suite/public-alpha-live-current.json` with `live_smoke_status: passed`, one live test, zero failures, and zero skips. The live XCTest log now includes an `XCSTEWARD_LIVE_SMOKE_EVIDENCE` line for job `15df29a1-f080-48b1-bd49-a22006e66199`, including summary/run-metadata/`.xcresult` paths, `tests_run=1`, and `probe_warnings=0`.
  - Progress: 2026-05-25 live-inclusive hardening matrix dogfood added one xcode-managed live smoke job, bringing real local jobs to 125. `scripts/run-hardening-matrix.sh --include-live` produced `.build/hardening-matrix/public-alpha-live-current.json` with `status: passed`, `live_included: true`, `live_skipped: false`, 65 rows, and zero failures. The live row job `f93e3807-cfef-4927-8817-885582a59890` succeeded with `tests_run=1`, `probe_warnings=0`, valid `.xcresult`, and build/test/artifact commands exiting 0.
  - Progress: 2026-05-25 `Recursia` follow-up dogfood converted the project from preflight-only to terminal-job coverage, bringing terminal-project coverage to 16 and real local jobs to 126. `doctor --project recursia-kanban --json --progress` passed state, Xcode/CoreSimulator, scheme, runnable destination, package, queue, and lease checks, then failed `.xctestrun` integrity with retained evidence and a `failure_excerpt` for the known project compile issue. Targeted submit job `4f59fbca-8fcb-4e58-a8a9-50dd15a46618` failed as `build_failure` after 76.7s with xcodebuild exit 65, job-local DerivedData, build/combined logs, command events, run metadata, `xcodebuild-help.txt`, empty `probe_warnings`, and summary preserved. A first post-run doctor saw one transient CoreSimulator JSON health timeout; immediate raw `simctl list --json` and a second doctor passed, and both doctor runs reported no stale worker lease or simulator leases.
  - Progress: 2026-05-25 `swift-cyment-arrow` UIKit demo follow-up dogfood added a clean non-Demo package-backed success path with live progress, bringing real local jobs to 127. `doctor --project cyment-arrow-uikit-tests --json --progress` passed every global and project check, including `.xctestrun` generation and bootstatus. Submit job `79fbd838-9386-46c0-96de-27b6a5fa5869` succeeded after 30.4s with 3 tests, 0 failures, valid `.xcresult`, JUnit, job-local DerivedData, build/test/combined logs, command events, run metadata, `xcodebuild-help.txt`, xcodebuild build/test exit 0, `xcresulttool` summary exit 0 under the derived 150s artifact timeout, and empty `probe_warnings`. Post-run doctor returned `overall_status: pass` with no runner contention, no stale worker lease, no simulator leases, and healthy CoreSimulator JSON enumeration.
  - Progress: 2026-05-25 cleanup dry-run dogfood exercised the public-alpha evidence-retention cleanup path without deleting artifacts. Against the 21 GB dogfood state root, `cleanup --dry-run --older-than 0s --keep-last 20 --max-total-size 8gb --json` reported `dry_run: true`, `deleted_count: 0`, `candidate_count: 98`, `selected_bytes: 11434037248`, `total_managed_bytes: 22453362688`, and every candidate had `deleted: false`. Recent success/failure summaries remained present after the dry-run, and follow-up doctor returned `overall_status: pass` with no stale worker lease, no simulator leases, no runner contention, and healthy CoreSimulator JSON enumeration.
  - Progress: 2026-05-25 evidence-discovery dogfood exercised `jobs --json`, `status --json`, `artifacts --json`, and `logs` against the accumulated state root after cleanup dry-run. `jobs --json` returned 118 terminal records: 84 succeeded, 31 failed, and 3 canceled, with no queued/running stragglers. Sampled success, failure, and canceled jobs all had discoverable status/artifact/log evidence; failed and canceled `status --json` exited non-zero while still returning structured JSON summaries. Follow-up doctor returned `overall_status: pass` with no stale worker lease, no simulator leases, no runner contention, healthy CoreSimulator JSON enumeration, and acceptable disk pressure at 23.0% free.
  - Progress: 2026-05-25 `CucumberSwift` follow-up dogfood added another package-backed non-Demo success path, bringing real local jobs to 128. `doctor --project cucumber-swift-dsl --json --progress` passed every global and project check, including package resolution, runnable destination, `.xctestrun` generation, `xcresulttool`, and bootstatus. Submit job `d6037853-3b78-479f-88dd-3e3d40309545` succeeded after 40.6s with 18 tests, 0 failures, valid `.xcresult`, JUnit, job-local DerivedData, build/test/combined logs, command events, run metadata, `xcodebuild-help.txt`, xcodebuild build/test exit 0, `xcresulttool` summary exit 0 under the derived 150s artifact timeout, and empty `probe_warnings`. Post-run doctor returned `overall_status: pass` with no runner contention, no stale worker lease, no simulator leases, healthy CoreSimulator JSON enumeration, and disk pressure acceptable at 22.8% free.
  - Progress: 2026-05-25 `batch-bg-remover` PATH-invocation dogfood reran the prior worker self-exec/orphaned-queue failure mode from the external repo, bringing real local jobs to 129. Bare `xcsteward --state-root /Users/acyment/dev/batch-bg-remover/.xcsteward doctor --project bgremover-app --json --progress` passed all global and project checks, including `.xctestrun` generation, `xcresulttool`, and bootstatus. Bare `xcsteward --state-root ... submit --project bgremover-app --wait --json --progress --only-testing BGRemoverAppUITests/BGRemoverAppUITests/testScenarioLaunchShowsBackgroundRemovalToolControls` created job `1428252d-0e7b-42be-9c66-36c258de9636`, returned terminal success instead of a bootstrap error, and preserved valid `.xcresult`, JUnit, job-local DerivedData, command events, run metadata, `xcodebuild-help.txt`, and empty `probe_warnings`; `jobs --json` in that external state root showed 22 terminal jobs with no queued/running leftovers. Post-run doctor reported no stale worker lease and no simulator leases; its only warning was unrelated direct macOS `xcodebuild` activity, matching the documented macOS destination capability gap.
  - Progress: 2026-05-25 external `batch-bg-remover` evidence-retention dogfood exercised the state root that previously contained the artifact-classification failure. `jobs --json` returned 22 terminal records with no queued/running leftovers; the historical `artifact_failure` job `9b7b3431-798d-4aa1-8c19-39d2a661a000` still returned structured status/artifact JSON, and manual `xcresulttool` read its retained `.xcresult` as `Passed` with `totalTestCount: 1`. `cleanup --dry-run --older-than 0s --keep-last 5 --max-total-size 2gb --json` reported `dry_run: true`, `deleted_count: 0`, `candidate_count: 17`, `selected_bytes: 3782385664`, `total_managed_bytes: 4920422400`, and every candidate had `deleted: false`; key old and latest summaries remained present afterward. Post-run doctor reported no stale worker lease and no simulator leases, with only unrelated direct macOS `xcodebuild` activity warning.
  - Progress: 2026-05-25 external `batch-bg-remover` safe-remediation dogfood ran `doctor --fix --json --progress` against a healthy real state root. It returned `overall_status: pass`, every check had `fixed: false`, and the queue stayed unchanged at 22 terminal records: 21 successes and 1 historical `artifact_failure`. Key old/latest summaries remained present, no stale worker lease or simulator leases were recorded, and a post-check process listing showed only `xcodebuildmcp` helpers matching the search term rather than direct `xcodebuild` test runners.
  - Progress: 2026-05-25 native macOS destination dogfood brought real local jobs to 130 and confirmed the remaining macOS capability gap against `BGRemoverMacApp`. Raw `xcodebuild -showdestinations` listed only `platform:macOS` destinations; `doctor --project bgremover-mac-app --json --progress` found the scheme and passed package resolution, then failed clearly at `project.showdestinations_runnable` rather than misreporting a missing scheme. Bounded submit job `f92157d5-f295-4f29-a61a-afedd70175a0` failed terminally as `build_failure` with xcodebuild exit 70, preserved build/combined logs, command events, job-local DerivedData, run metadata, summary, and empty `probe_warnings`, and post-run doctor reported no stale worker lease or simulator leases. Follow-up implementation now prevents the observed simulator-backed fallback by returning `unsupported_destination` before simulator mutation, so native macOS support still needs a simulator-free execution path after simulator-only alpha validation.
  - Progress: 2026-05-25 post-macOS-gap recovery dogfood brought real local jobs to 131. After the unsupported macOS-only scheme failure, `doctor --project demo-app --json --progress` passed state root, CoreSimulator JSON health, runner contention, worker lease, simulator leases, scheme, runnable destination, package resolution, `.xctestrun`, `xcresulttool`, and bootstatus checks, with only the known xcode-managed parallelism warning. Targeted submit job `fd40ed82-a32c-4486-ab7f-56c7a945642c` succeeded after 55.3s with 1 test, valid `.xcresult`, JUnit, job-local DerivedData, command events, run metadata, `xcodebuild-help.txt`, and empty `probe_warnings`; manual `xcresulttool` read `Passed` with `totalTestCount: 1`. Post-run global doctor passed with no stale worker lease, no simulator leases, no runner contention, and healthy CoreSimulator JSON enumeration.
  - Progress: 2026-05-26 `lernit` large-suite rerun brought real local jobs to 132. `doctor --project lernit --json --progress` passed state root, CoreSimulator JSON health, runner contention, worker lease, simulator leases, scheme, runnable destination, package resolution, `xcresulttool`, and bootstatus checks, but failed `project.xctestrun_integrity` with retained linker evidence. Submit job `75ac5ccb-f4d2-46ce-a495-f24b172b71e4` still progressed through successful `build-for-testing`, ran 334 tests for 821.4s, and reached terminal `test_failure` with xcodebuild exit 65, valid `.xcresult`, JUnit, job-local DerivedData, build/test/combined logs, command events, run metadata, and empty `probe_warnings`. Manual `xcresulttool` summary reported 326 passed tests and 8 failures, including 5 per-test execution-time failures in `BinaryDragDropViewTests`; the test log stayed quiet until Xcode emitted the final `Testing started completed` line at 782.5s. Post-run global doctor returned `overall_status: warn` only for unrelated direct macOS `xcodebuild` contention and reported no stale worker lease, no simulator leases, healthy CoreSimulator JSON enumeration, and acceptable disk pressure.
  - Progress: the `lernit` doctor/submit mismatch is now fixed generically. Doctor carries forward the first concrete iOS Simulator destination ID from `xcodebuild -showdestinations` and uses it for the `.xctestrun` integrity `build-for-testing` probe instead of `generic/platform=iOS Simulator`, avoiding false x86_64 simulator link failures on Apple Silicon projects with arm64-only native dependencies. Focused tests cover the concrete-destination behavior, and a live rerun of `doctor --project lernit --json --progress` now returns `overall_status: pass` with `.xctestrun generated successfully at Lernit_Lernit_iphonesimulator26.5-arm64.xctestrun`.
  - Progress: 2026-05-26 post-fix `lernit` targeted dogfood brought real local jobs to 133. `doctor --project lernit --json --progress` returned `overall_status: pass`, then targeted submit job `1f935c5c-bc7e-4ea8-99cf-3600075ec7e4` succeeded after 77.2s with `--only-testing LernitTests/VideoScreenTests/test_givenVideoScreen_thenBackButtonExists`, xcodebuild exit 0, valid `.xcresult`, JUnit, job-local DerivedData, build/test/combined logs, command events, and run metadata preserved. Manual `xcresulttool` summary reported `Passed` with `totalTestCount: 1`, and post-run global doctor reported no stale worker lease and no simulator leases; its only warning was unrelated direct macOS `xcodebuild` contention from `BGRemoverMacApp`.
  - Progress: 2026-05-26 live same-simulator concurrency dogfood on `lernit` brought real local jobs to 135. Two concurrent `submit --wait --json --progress` clients targeted `LernitTests/VideoScreenTests/test_givenVideoScreen_thenBackButtonExists` and `LernitTests/VideoPrewarmTests/test_putThenTakeReturnsController`; both jobs succeeded with valid `.xcresult`, JUnit, job-local DerivedData, build/test/combined logs, command events, and run metadata. Serialization held on the shared simulator instead of failing fast or overlapping mutation: job `85cd4ee4-5717-48df-b829-1ba8418c72a1` test execution finished at `1779796465.611965`, while job `9e0dcf95-53c3-4a28-b2a1-3422030bb1be` did not launch its first tool command until `1779796467.898035` and did not start build until `1779796469.8049421`. Post-run global doctor returned `overall_status: pass` with no competing runner processes, no stale worker lease, and no simulator leases.
  - Progress: 2026-05-26 live interrupted-wait-client dogfood on `lernit` brought real local jobs to 137. A warm control run first produced job `975f43f0-a221-412f-8f0f-167eb11c811a`, which succeeded after 47.3s with 1 targeted test and the expected artifact set. The follow-up run sent `^C` to a live `submit --wait --json --progress` PTY after job `7e04197d-e91f-4053-97e4-3039cfae1570` had entered `running`; the waiting client exited early, but `status --json` showed the job remained active, `command-events.jsonl` showed build launched at `1779797444.914857` and test finished at `1779797488.2453032` after the client exit, and the final job summary reached `succeeded` with 1 targeted test, valid `.xcresult`, JUnit, job-local DerivedData, build/test/combined logs, command events, and run metadata. Manual `xcresulttool` summary returned `Passed`, and post-run global doctor reported no competing runner processes, no stale worker lease, and no simulator leases.
  - Progress: 2026-05-26 live queued-cancel dogfood on `lernit` brought real local jobs to 139. `doctor --project lernit --json` passed before the run. Targeted job `364812cc-6b36-4241-87e8-0b26f29d785a` started first and later succeeded after 49.0s with 1 targeted test, valid `.xcresult`, JUnit, job-local DerivedData, build/test/combined logs, command events, and run metadata; manual `xcresulttool` summary returned `Passed`. Same-simulator job `7ac4b04a-ff23-4c2b-b99f-7c74e9e90613` was canceled while still `queued`; terminal `status --json` reported `state: canceled`, `result_class: canceled`, `duration_seconds: 0`, and no `started_at` or `simulator_id`, while `artifacts --json` returned `{}`. After the active job finished, `jobs --json` showed both terminal outcomes with no queued or running leftovers, and post-run global doctor returned `overall_status: pass` with no competing runner processes, no stale worker lease, and no simulator leases.
  - Progress: 2026-05-26 package-backed same-simulator concurrency dogfood on `cucumber-swift-dsl` brought real local jobs to 141. `doctor --project cucumber-swift-dsl --json --progress` passed every global and project check, including package resolution, runnable destination, `.xctestrun` integrity, `xcresulttool`, and bootstatus. Two concurrent full-suite `submit --wait --wait-timeout 900 --json --progress` clients then both succeeded: job `a6337a63-996b-465f-954b-9f9b00f974a0` finished first with 18 tests after 100.0s, and job `22396e0c-f47f-4661-91a0-efcb149d4370` finished second with 18 tests after its own 70.4s run. Serialization again held on the shared simulator instead of overlapping mutation: the first job's test phase finished at `1779799643.504908`, while the second job stayed queued for about 113.9s, did not launch its first tool command until `1779799655.8512568`, and did not start `xcodebuild build-for-testing` until `1779799667.469514`. Both preserved valid `.xcresult`, JUnit, job-local DerivedData, build/test/combined logs, command events, and run metadata; manual `xcresulttool` summaries returned `Passed` with `totalTestCount: 18` for both jobs. Post-run global doctor returned `overall_status: pass` with no competing runner processes, no stale worker lease, no simulator leases, and `jobs --json` showed no queued or running leftovers.
  - Progress: 2026-05-26 live worker-crash recovery dogfood on isolated state root `/private/tmp/xcsteward-live-worker-crash-2026-05-26` brought real local jobs to 143 and cancellation/timeout/crash scenarios to 11. Preflight `doctor --project lernit --json --progress` passed. Crash job `5ec3434c-3101-4f81-9e23-44dbfe231108` entered `running`, recorded child PID `80261`, then the singleton worker PID `80112` was killed intentionally; after follow-up submit spawned a new worker, the first job reached terminal `interrupted` with summary `Interrupted: worker process exited before the job completed`, the orphaned `xcodebuild build-for-testing` process disappeared from `ps`, and same-simulator follow-up job `0ec04188-8977-426c-bd7d-441816306fbe` succeeded with 1 targeted test, valid `.xcresult`, JUnit, command events, run metadata, and xcodebuild exit 0. `jobs --json` in the isolated state root showed exactly one `interrupted` plus one `succeeded` terminal job, and final doctor returned `overall_status: pass` with no stale worker lease and no simulator leases.
  - Progress: 2026-05-26 live worker-crash-during-test dogfood on isolated state root `/private/tmp/xcsteward-live-worker-crash-test-2026-05-26` brought real local jobs to 146 and cancellation/timeout/crash scenarios to 12. Preflight `doctor --project lernit --json --progress` passed. A short targeted control job `02303c3e-01d2-4f23-9915-7ab5371b8088` succeeded first so the isolated state root had a known-good baseline. Full-suite crash job `30408db2-b6e4-44dd-b187-743276758814` then advanced into real `xcodebuild ... test-without-building` with child PID `7439`; the singleton worker PID `5731` was killed intentionally at that point, after which the first job reached terminal `interrupted` with summary `Interrupted: worker process exited before the job completed`, the orphaned test-runner PID disappeared from `ps`, and same-simulator follow-up job `8a64b9fa-1966-4bd4-a31c-5707cbeaeea3` succeeded with 1 targeted test, valid `.xcresult`, JUnit, command events, run metadata, and xcodebuild exit 0. `jobs --json` in the isolated state root showed one control success, one `interrupted`, and one follow-up success with no queued or running leftovers, and final doctor returned `overall_status: pass` with no stale worker lease and no simulator leases.
  - Progress: 2026-05-26 live stale-worker/backpressure dogfood on isolated state root `/private/tmp/xcsteward-live-backpressure-2026-05-26` brought real local jobs to 149 and cancellation/timeout/crash scenarios to 13. Preflight `doctor --project lernit --json --progress` passed. Full-suite job `f3e8d1a6-7af8-4788-9d10-3c8e8511c2db` then entered real `xcodebuild ... test-without-building` with child PID `40466`; after the singleton worker PID `39268` was killed, the dead worker lease lingered while the orphaned child kept running under PID 1 until same-simulator follow-up submit `0e498428-2ffa-4f43-b917-e70a3c13ca4d` triggered stale-worker recovery. At that point the first job reached terminal `interrupted`, the orphaned child PID disappeared from `ps`, and the follow-up job succeeded with valid `.xcresult`, JUnit, command events, run metadata, and xcodebuild exit 0. The same isolated root then live-validated strict foreign-activity backpressure: while a real foreign `xcodebuild -xctestrun ... test-without-building` process was active, submit `c6fd66a4-79ff-481b-9106-9306eec45123` stayed queued for more than 80 seconds with no process ID while `host-health.json` reported `effective_max_jobs: 0`, `foreign_activity_policy: "strict"`, `foreign_runner_process_count: 1`, and reason `foreign_runner_processes=1`; after the foreign runner exited, `host-health.json` flipped back to `effective_max_jobs: 1` / `foreign_runner_process_count: 0`, the queued job started automatically, and it reached terminal `succeeded` with valid `.xcresult`, JUnit, command events, run metadata, and xcodebuild exit 0. Final doctor returned `overall_status: pass` with no stale worker lease, no simulator leases, and no competing runner processes.
  - Progress: 2026-05-26 live wait-client stale-worker recovery dogfood on isolated state root `/private/tmp/xcsteward-live-waiter-recovery-2026-05-26` brought real local jobs to 150, induced failures to 23, and cancellation/timeout/crash scenarios to 14. Preflight `doctor --project lernit --json --progress` passed. Full submit job `b09d8955-e99b-4960-842a-c2e463308f3e` reached `running` with job-owned child PID `8543` executing `xcodebuild ... build-for-testing`; after singleton worker PID `8473` was killed, the same `submit --wait --json --progress` client reconciled the dead lease without any follow-up submit, returned terminal `interrupted` after 31.7s with summary `Interrupted: worker process exited before the job completed`, and preserved logs, command events, job-local DerivedData, and result bundle path. PID `8543` was absent from `ps`, the state database showed no worker lease or simulator lease leftovers, and final doctor returned `overall_status: pass` with no stale worker lease, no simulator leases, no competing runner processes, healthy CoreSimulator JSON enumeration, and `.xctestrun` integrity passing.
  - Progress: 2026-05-26 same-root follow-up after waiter-side stale-worker recovery brought real local jobs to 151. Starting from isolated root `/private/tmp/xcsteward-live-waiter-recovery-2026-05-26` with one terminal interrupted job and no worker/simulator leases, targeted follow-up job `f3ebf55a-9bd3-4b15-8ba1-4c2ffa200acd` started normally on the same simulator and succeeded after 100.5s with xcodebuild exit 0, `testsRun: 1`, valid `.xcresult`, JUnit, logs, command events, job-local DerivedData, and run metadata. Manual `xcresulttool` summary reported `Passed` with `totalTestCount: 1`; final state had no queued/running leftovers, no worker lease, no simulator lease rows, and post-run doctor returned `overall_status: pass`.
  - Progress: 2026-05-26 DemoApp destination-classification dogfood brought real local jobs to 153 and found/fixed a submit/doctor mismatch. Preflight `doctor --project demo-app --json --progress` on the long-lived dogfood root returned `overall_status: warn` only for the known xcode-managed parallelism warning and reported a runnable iOS Simulator destination. Submit job `c34a45eb-4c86-4e25-a5bd-6382de9a1b3d` then failed as `unsupported_destination` because raw `xcodebuild -showdestinations` exposed macOS plus only the iOS Simulator placeholder; this was not a native macOS-only scheme. Submit now avoids classifying placeholder-only iOS Simulator output as macOS-only, covered by `ExecutionOutcomeE2ETests/testPlaceholderIOSSimulatorDestinationIsNotClassifiedAsMacOSOnly`. Rerun job `cf481fb2-d2a8-46c0-8111-70e0503bb344` resolved simulator `CBB8584A-F555-4C67-B700-6F858ACAE0E8`, succeeded after 30.6s with 1 test, valid `.xcresult`, JUnit, logs, command events, job-local DerivedData, run metadata, and xcodebuild exit 0; manual `xcresulttool` summary returned `Passed` with `totalTestCount: 1`, and post-run doctor reported no stale worker lease, no simulator leases, no runner contention, healthy CoreSimulator JSON enumeration, and the same known parallelism warning only.
  - Progress: 2026-05-26 post-fix DemoApp stability dogfood brought real local jobs to 154. Preflight and post-run `doctor --project demo-app --json --progress` on the long-lived dogfood root returned `overall_status: warn` only for the documented xcode-managed parallelism warning, while state, Xcode/CoreSimulator, scheme, runnable destination, package, `.xctestrun`, `xcresulttool`, worker lease, simulator lease, and bootstatus checks passed. Submit job `a540bf9e-d279-4bce-86a6-694c3dc475b4` succeeded after 30.5s with 1 targeted test, xcodebuild build/test exit 0, valid `.xcresult`, JUnit, command events, job-local DerivedData, run metadata, derived 150s artifact probe exit 0, and empty `probe_warnings`; `jobs --json` showed no queued/running leftovers.
  - Progress: 2026-05-26 true native-macOS unsupported-path dogfood brought real local jobs to 155. Doctor for `bgremover-mac-app` found the `BGRemoverMacApp` scheme and passed package resolution, then failed only `project.showdestinations_runnable` because no runnable iOS Simulator destination exists. Submit job `9ec8cac7-209f-45d6-a0ea-2f068ff5fc07` reached terminal `unsupported_destination` after 6.6s with the public-alpha scope message, `simulator_id: ""`, summary/run metadata, command events, and logs preserved. Command events proved the runner stopped after `xcodebuild -showdestinations`: no `simctl`, no simulator lease, no `build-for-testing`, no test command, and no `.xcresult` path; post-run doctor reported zero worker leases and zero simulator leases.
  - Progress: 2026-05-26 routine volume dogfood batch added five clean targeted DemoApp runs, bringing real local jobs to 160. Preflight doctor returned only the known xcode-managed parallelism warning; all five jobs succeeded with xcodebuild exit 0, valid `.xcresult`, JUnit, command events, run metadata, and empty `probe_warnings`. Post-run global doctor returned `overall_status: pass` with no stale worker lease, no simulator leases, no runner contention, and 140 terminal jobs (101 succeeded, 35 failed, 4 canceled).
  - Tracking: live dogfood progress is now recorded in `docs/dogfood-ledger.md`.

- [x] Add opt-in live CLI progress for long-running JSON commands.
  - Severity: S1 observability gap.
  - Evidence: live dogfood showed `doctor --json` and `submit --wait --json` can be silent for long enough that users cannot distinguish healthy CoreSimulator/Xcode progress from a hang.
  - Acceptance criteria: `doctor --json --progress` and `submit --wait --json --progress` keep stdout as the final JSON object, emit incremental machine-readable events to stderr, and tests prove progress events do not break final JSON parsing.
  - Progress: `--progress` now writes compact JSON-lines events on stderr for doctor check start/finish and submit worker/job status changes, including terminal status.

- [x] Clarify unsupported native macOS destination UX before public alpha.
  - Severity: S1 public-alpha scope clarity; not an S0 safety issue.
  - Acceptance criteria: public-alpha docs state that the initial launch is simulator-only, native macOS schemes are listed in known limitations, and `doctor`/`submit` fail clearly for macOS-only schemes instead of presenting them as missing schemes or generic project failures.
  - Progress: docs now position native macOS destination support as post-alpha roadmap work rather than part of the simulator-only launch. Doctor finds macOS-only schemes and fails the runnable iOS Simulator destination check clearly, while submit now preflights `xcodebuild -showdestinations` before simulator resolution and finishes macOS-only schemes as `unsupported_destination` with preserved summary/run metadata, no simulator ID, no simulator lease, no `simctl list`/boot/shutdown, and no `build-for-testing`.
  - Progress: submit no longer treats macOS plus placeholder-only iOS Simulator destinations as native macOS-only. Placeholder-only simulator output now continues down the simulator path, where configured simulator resolution can proceed; true macOS-only schemes still fail before simulator mutation.
  - Progress: live dogfood confirmed both sides of the boundary: placeholder-only iOS Simulator output for `demo-app` now proceeds and succeeds on a configured simulator, while true macOS-only `BGRemoverMacApp` stops as `unsupported_destination` after `xcodebuild -showdestinations` with no simulator mutation.

- [ ] Support native macOS app destinations without simulator assumptions.
  - Severity: S1 post-alpha capability gap; not a public-alpha blocker and not an S0 safety issue.
  - Sequence: ship the simulator-only public alpha first, then implement `platform=macOS` after alpha validation.
  - Evidence: dogfood confirmed XCSteward can verify the iPhone simulator path, but native macOS app schemes still require direct `xcodebuild -destination 'platform=macOS'` because XCSteward currently assumes simulator-backed execution, leases, and destination validation.
  - Acceptance criteria: profiles can declare a macOS destination, doctor validates macOS schemes without CoreSimulator runnable-destination requirements, submit builds/tests with `-destination platform=macOS`, and executor skips simulator lease/boot/shutdown/diagnose paths for macOS jobs while preserving the same artifacts and result classification.
  - Progress: 2026-05-25 dogfood reproduced this against `/Users/acyment/dev/batch-bg-remover` scheme `BGRemoverMacApp`. Doctor behaved clearly by failing only the iOS Simulator runnable-destination check after confirming the scheme exists. Submit now guards this scope boundary by returning `unsupported_destination` before simulator mutation; the post-alpha gap is real macOS execution support, not simulator-backed fallback behavior.

- [x] Reduce noise from live `xcodebuild -help` capability probe timeouts.
  - Severity: S2 observability polish; not a public-alpha safety blocker while recorded as a warning and not used to classify the job as failed.
  - Evidence: live DemoApp progress-stream dogfood job `354741a7-c611-41b8-a491-4e866a596e49` succeeded, but run metadata included a non-fatal `probe_warnings` entry for `xcodebuild.help` timing out with exit 143. The core build, test, and `xcresulttool` summary commands all exited 0.
  - Acceptance criteria: repeated live runs should either avoid probing `xcodebuild -help` on every job, cache capability evidence per selected Xcode version, or retry/extend the bounded probe so successful jobs do not routinely carry noisy capability-probe warnings.
  - Progress: `xcodebuild -help` evidence capture now retries a timed-out 5s probe once with a 30s timeout before recording a warning; non-timeout failures still produce warnings immediately.
  - Evidence: `ResultReporterTests/testRetriesTimedOutXcodebuildHelpProbeBeforeRecordingWarning` covers the transient timeout path and proves the retried help artifact is written with no `probe_warnings`.
  - Validation: live DemoApp job `acaf98ff-def1-4eca-8267-50dd734a6b7a` captured `artifacts/xcodebuild-help.txt` and ended with an empty `probe_warnings` array while preserving successful build/test/artifact command evidence.

- [x] Retain actionable build-for-testing evidence for doctor `.xctestrun` integrity failures.
  - Severity: S1 diagnostic gap, not an S0 safety blocker.
  - Evidence: live `Recursia` and broad `CucumberSwift` preflights failed `project.xctestrun_integrity` with the generic message `build-for-testing failed while validating .xctestrun generation`; the root compiler errors were only visible after manually rerunning the equivalent `xcodebuild build-for-testing`.
  - Acceptance criteria: when the doctor `.xctestrun` integrity probe fails, JSON output includes either a retained log path or a concise captured failure excerpt, the scratch cleanup policy preserves that evidence without leaving large DerivedData behind, and tests cover both failure and timeout behavior.
  - Progress: `project.xctestrun_integrity` failures/timeouts now include `evidence_path` and `failure_excerpt` in JSON output, plain doctor output prints the evidence path/detail, and the retained evidence is a capped `build-for-testing` log outside the scratch DerivedData directory.
  - Evidence: `DoctorProjectPreflightCommandTests/testDoctorRetainsBuildForTestingEvidenceWhenXCTestRunIntegrityBuildFails` and `testDoctorWarnsWhenXCTestRunIntegrityBuildTimesOut` cover failure and timeout evidence retention while proving scratch DerivedData is still removed.
  - Validation: rerunning live `doctor --project cucumber-swift --json` now reports `failure_excerpt` as the real `Bundle.module` compiler error and writes `evidence_path` under the dogfood state root without leaving scratch DerivedData.

- [x] Reject wrong-typed known profile values instead of silently using defaults.
  - Severity: S1.
  - Acceptance criteria: if a known profile key is present with the wrong TOML type, profile loading fails with a clear `invalidConfiguration` message and the worker records `runner_bootstrap_failure` before simulator or xcodebuild mutation.
  - Evidence: live dogfood profile `demo-malformed-profile` set `parallel.max_workers = "one"` and still ran successfully, which is conservative for that key but too surprising for public-alpha profile validation.
  - Progress: root string/array values and older defaulted sections such as `[parallel]`, `[timeouts]`, `[test_timeouts]`, `[test_retries]`, `[destination]`, `[ports]`, and `[managed_simulator]` now distinguish absent values from present wrong-typed values.
  - Progress: profile TOML parsing now accepts inline comments after values and section headers while preserving `#` characters inside quoted strings and arrays.
  - Progress: profile TOML arrays are now quote-aware, preserve commas inside quoted values, allow trailing commas, and reject unquoted array entries instead of silently treating them as strings.
  - Evidence: `ProfileLoaderTests`, `ProfileSectionDecodersTests`, and `ProfileFailureRecoveryTests` cover loader-level messages and worker-level pre-mutation failure; the live rerun failed as `runner_bootstrap_failure`.

- [x] Diagnose live DemoApp destination enumeration regression.
  - Severity: S1 dogfood blocker if persistent.
  - Acceptance criteria: `doctor --project demo-app` either passes `project.showdestinations_runnable` again or records a clear actionable root cause for why Xcode exposes only the iOS Simulator placeholder while `simctl` sees the configured booted device.
  - Evidence: after the validation-gap fix, two live doctor runs failed `project.showdestinations_runnable`; raw `xcodebuild -showdestinations` for `Examples/DemoApp` listed My Mac, a physical iPhone, Any iOS Device, and Any iOS Simulator Device, but no concrete simulator destination.
  - Resolution: rerun raw `xcodebuild -showdestinations` now lists concrete iOS 26.5 simulators, including `CBB8584A-F555-4C67-B700-6F858ACAE0E8`, and live `doctor --project demo-app --json` passes `project.showdestinations_runnable`. The suspected root cause is transient Xcode/CoreSimulator destination enumeration state, not the DemoApp profile or scheme.
  - Progress: placeholder-only `-showdestinations` output now has a specific doctor failure message and manual action instead of the generic no-runnable-simulator wording.

- [x] Complete public-alpha install, onboarding, and limitation docs.
  - Severity: S1 public-alpha gate.
  - Acceptance criteria: one supported install path, one quickstart, one real project profile example, one agent workflow example, uninstall/cleanup instructions, and a known-limitations section covering supported Xcode/macOS scope, serialized execution defaults, experimental concurrency, and live-smoke expectations.
  - Evidence: `README.md` links the public-alpha docs; `docs/public-alpha.md` covers install, quickstart, real project profile, agent workflow links, release-gate commands, known limitations, and uninstall/cleanup.

- [x] Reconcile dead worker leases without requiring new queue traffic.
  - Severity: S1 recovery/observability gap; not yet observed as data loss, but it can leave a `submit --wait` client watching a dead worker lease until another submit triggers stale-worker recovery.
  - Evidence: live dogfood on 2026-05-26 killed singleton worker PID `39268` while job `f3e8d1a6-7af8-4788-9d10-3c8e8511c2db` still had child PID `40466` running `xcodebuild ... test-without-building`. The job remained `running` with a dead lease and orphaned child under PID 1 until follow-up submit `0e498428-2ffa-4f43-b917-e70a3c13ca4d` arrived and triggered stale-worker recovery; only then did the first job become terminal `interrupted`.
  - Acceptance criteria: once the worker heartbeat is stale and the lease owner PID is dead, XCSteward should reconcile the dead worker/job without waiting for another submit path to touch the queue; `submit --wait` should observe terminal interruption promptly, orphaned runner cleanup should still stay conservative, and tests should cover both autonomous recovery and follow-up-submit recovery.
  - Progress: `submit --wait` now performs the same conservative stale-worker, unowned-running-job, and stale-simulator-lease reconciliation while polling. If recovery leaves queued work behind, the waiter respawns the singleton worker using the already-resolved executable path.
  - Evidence: `WorkerCrashRecoveryE2ETests/testSubmitWaitClientRecoversDeadWorkerWithoutFollowUpSubmit` recreates the live failure shape with one waiting client, kills the worker during fake `test-without-building`, verifies the waiter returns terminal `interrupted` JSON without a second submit, and confirms the orphaned runner process and leases are gone.

- [ ] Define paid early-access gate separately from public alpha.
  - Severity: S1/S2, not a public-alpha blocker.
  - Acceptance criteria: 5+ external design partners, 5+ real projects, 500+ real jobs, zero S0 user-reported incidents, zero unrecoverable queue incidents, zero false-success incidents, zero artifact-loss incidents, repeatable install without maintainer help, and support issues diagnosable from run artifacts.

### High-ROI refactor opportunities

- [x] Split `DoctorEngine` into focused check groups and shared probe helpers.
  - `Sources/XCStewardKit/Doctor.swift`
  - Evidence: the file is 2,010 lines with the highest source branch density found in the Swift sources; it mixes check registration, Xcode/CoreSimulator probes, JSON parsing, path safety, disk checks, and report construction.
  - Target slice: move global Xcode environment checks, CoreSimulator checks, project preflight checks, and shared `DoctorCheck` construction/probe result handling into focused types while keeping check IDs and output stable.
  - ROI: every new doctor check currently increases risk in one large file; this should reduce merge conflicts and make targeted tests such as `DoctorCommandTests` and `DoctorCheckRegistryTests` cheaper to reason about.
  - Progress: path-safety warning checks are split into `DoctorPathSafety` so protected state-root/project path policy can evolve outside the main doctor engine.
  - Progress: disk free-space and disk-pressure checks are split into `DoctorDiskHealth`, leaving global check IDs and doctor JSON behavior stable.
  - Progress: shared JSON probe execution and probe failure classification are split into `DoctorJSONProbeRunner`, leaving CoreSimulator doctor checks on the same typed probe behavior.
  - Progress: Xcode selection, first-launch, simulator SDK, and CLI-alignment checks are split into `DoctorXcodeEnvironment`, keeping global check IDs and simctl-dependent skip behavior stable.
  - Progress: global CoreSimulator runtime, JSON health, and unavailable-device cleanup checks are split into `DoctorCoreSimulatorHealth`, with project bootstatus preflights reusing the same simctl-availability skip behavior.
  - Progress: project repo/scheme/destination/test-plan/package/DerivedData/`.xctestrun`/`xcresulttool`/managed-simulator preflights are split into `DoctorProjectPreflight`, leaving `DoctorEngine` focused on registry orchestration and state/lease health.
  - Progress: state-root, worker-lease, simulator-lease, and competing-runner checks are split into `DoctorStateHealth`; `DoctorEngine` is now registry/report orchestration at 201 lines.

- [x] Factor shared xcodebuild attempt, retry, and artifact-preservation flow.
  - `Sources/XCStewardKit/Executor.swift`
  - `Sources/XCStewardKit/ManualShardRunner.swift`
  - `Sources/XCStewardKit/ExecutionSupport.swift`
  - Evidence: xcode-managed tests and manual shards both remove result bundles, run xcodebuild, classify outcomes, preserve first-attempt artifacts, record diagnostics, recover simulators, and retry bootstrap/artifact failures.
  - Target slice: introduce a small attempt runner/result object used by both single-run and shard-run paths; keep manual shard scheduling separate.
  - ROI: retry, cancellation, diagnostics, and artifact semantics are high-risk behavior, and duplicated flow makes fixes easy to apply to only one execution mode.
  - Progress: retry attempt evidence preservation is centralized in `ExecutionSupport` via `RetryAttemptEvidence`/`recordRetryAttemptEvidence` and is now used by both xcode-managed retries and manual shard retries.
  - Progress: xcodebuild test-attempt execution is centralized through `XcodebuildTestAttempt`/`runXcodebuildTestAttempt`, sharing simulator privacy prep, result-stream prep, test-runner environment construction, timeout selection, logging, and optional process-start tracking across xcode-managed and manual-shard runs.

- [x] Move CLI command parsers/handlers behind a command table.
  - `Sources/XCStewardKit/App.swift`
  - Evidence: `XCStewardApp` previously owned top-level dispatch, help text, option parsing, stdout formatting, worker spawning, cancellation, status, and artifacts in one switch-heavy entry point.
  - Target slice: move command parsers/handlers behind a command table now that cleanup selection/deletion lives in `CleanupService`.
  - ROI: command parsing can be tested without exercising the full CLI surface, and new commands will stop increasing the size of the app entry point.
  - Progress: top-level command lookup, command help routing, root-help command listing, and command dispatch now use a single `CLICommandDefinition` table while preserving existing handler behavior.
  - Progress: option consumption now reports missing option values explicitly, and submit/status/jobs/logs/artifacts/cancel/doctor/internal handlers reject leftover arguments instead of silently ignoring typoed flags.
  - Progress: global `--state-root` parsing now uses the same missing-value guard as command options, so `--state-root --json` fails as usage before state-root setup or queue mutation instead of treating `--json` as a filesystem path.
  - Progress: submit command option parsing moved into `SubmitCommandOptions` with direct unit coverage for repeated test filters, wait timeout parsing, JSON mode, and usage errors; `handleSubmit` now focuses on durable job creation, worker launch, wait behavior, and output.

- [x] Break the embedded fake tool scripts into scenario-oriented fixtures.
  - `Tests/XCStewardKitTests/FakeToolFixtures.swift`
  - Evidence: the file was 1,117 lines with one embedded fake-tool script block accounting for most branch-heavy test fixture logic; it is now 156 lines of scenario setup and shared install plumbing.
  - Target slice: split `xcodebuild`, `xcrun`, `ps`, and host-capacity fake tools into separate fixture builders or script assets, and centralize scenario names/capabilities.
  - ROI: most E2E changes depend on these fixtures; smaller fixtures should make failures easier to localize and reduce accidental scenario coupling.
  - Progress: process/host utility fake scripts (`ps`, `memory_pressure`, `pmset`, and `xcode-select`) are split into `FakeToolUtilityScripts`, leaving the main fixture file focused on scenario setup plus the large `xcodebuild`/`xcrun` scripts.
  - Progress: fake `xcodebuild` and `xcrun` are split into `FakeToolXcodebuildScript` and `FakeToolXcrunScript`; `FakeToolFixtures` now owns only the shared `FakeScenario`, `FakeToolEnvironment`, and install list plumbing.

- [x] Split `EndToEndCommandTests` by workflow using the existing `E2EScenario` helper.
  - `Tests/XCStewardKitTests/SubmitCommandE2ETests.swift`
  - `Tests/XCStewardKitTests/SimulatorBootstrapE2ETests.swift`
  - `Tests/XCStewardKitTests/CancellationE2ETests.swift`
  - `Tests/XCStewardKitTests/WorkerParallelE2ETests.swift`
  - `Tests/XCStewardKitTests/E2ECommandTestSupport.swift`
  - Evidence: after the managed-simulator, manual-shard, focused outcome/artifact/timeout, profile-validation, worker-scheduling, manual-shard execution/retry/cleanup, and xcode-managed configuration splits, the final 16 mixed tests were moved into focused suites and `EndToEndCommandTests.swift` was removed.
  - Target slice: move tests into focused files such as submit/artifacts, simulator management, manual shards, worker scheduling, cancellation, and cleanup while preserving helper APIs.
  - ROI: low behavior risk with immediate payoff in reviewability, targeted test runs, and conflict reduction.
  - Progress: managed-simulator parsing, create-failure, noisy-create, and cancellation E2E cases moved from `EndToEndCommandTests` into `ManagedSimulatorE2ETests` using `E2EScenario`, reducing the monolith while preserving assertions.
  - Progress: manual-shard argument propagation for destination timeouts, skip/test-configuration filters, coverage, result streams, result-bundle versions, retry mode, and diagnostics moved into `ManualShardConfigurationE2ETests`; the release-tier `manual-shards` group now includes the split class.
  - Progress: build/test timeout, build-failure, corrupt `.xcresult`, and runner-configuration outcome scenarios moved into focused `ExecutionOutcomeE2ETests`, `TimeoutHardeningE2ETests`, and `ResultArtifactE2ETests`; hardening-matrix rows now point at focused suites.
  - Progress: invalid profile/configuration cases for parallel mode, reset policy, ports, privacy, timeouts, coverage, result streams, result bundles, retries, diagnostics, and test-products runtime moved into `ProfileValidationE2ETests`, reducing duplicated CLI setup and shifting them to the `profile-recovery` release group; `profile-recovery` passed 24 tests and `e2e-command-surface` passed 55 tests in 188.218s.
  - Progress: worker scheduling, same-simulator serialization, host pressure, active-simulator lease budget, dynamic backpressure, capacity refill, and infrastructure-drain recovery moved into `WorkerSchedulingE2ETests`; hardening-matrix rows now point at the worker suite, `worker-and-capacity` passed 29 tests, and `e2e-command-surface` passed 47 tests in 93.549s.
  - Progress: manual-shard execution, test-products runtime, privacy application, merge failure, simulator ID validation, managed-simulator cloning, bootstrap retry diagnostics, retry artifact preservation, historical timing balance, hybrid mode, and erase cleanup moved into `ManualShardE2ETests`; `manual-shards` passed 21 tests and `e2e-command-surface` passed 36 tests in 67.308s.
  - Progress: xcode-managed test-plan leakage, parallel flags, timeout allowances, destination timeout, filters, coverage, result-stream, result-bundle, retry flag, diagnostics, port range, privacy, test-products, and generated `.xctestrun` path coverage moved into `XcodeManagedConfigurationE2ETests`; the release-tier `e2e-command-surface` group now includes the split class.
  - Progress: the last mixed workflow cases moved out: submit/help/artifact-baseline cases live in `SubmitCommandE2ETests`, simulator boot/retry/reset/competing-process cases live in `SimulatorBootstrapE2ETests`, queued cancellation lives in `CancellationE2ETests`, and remaining concurrent worker/manual-shard cases live in `WorkerParallelE2ETests`.
  - Resolution: `EndToEndCommandTests.swift` was removed; release groups now point at focused E2E suites.

### Test suite findings

- [x] Partition slow fake-tool integration suites into explicit fast and release-gate tiers.
  - Severity: S1 test-signal risk.
  - Evidence: the latest full `swift test` executed 353 tests, skipped 1 live smoke, and took 456.388s. `DoctorCommandTests` took 146.759s and `EndToEndCommandTests` took 210.535s, so those two suites account for roughly 78% of full-suite runtime.
  - Acceptance criteria: document a fast suite for normal refactor loops, keep a release-gate suite that still runs all fake-tool E2E/hardening coverage, group slow commands by workflow, and ensure no public-alpha release gate depends on remembering ad hoc filters.
  - Progress: `scripts/run-test-suite.sh` now exposes named `fast`, `release`, and `live` tiers, groups slow fake-tool suites by workflow, validates that the release tier covers every non-live `XCTestCase` class, and writes suite-health JSON reports under `.build/test-suite/`.
  - Progress: suite group filters are now validated against the repo's `XCTestCase` class list before SwiftPM runs, so a stale class mixed into a multi-filter group fails immediately instead of being hidden by other matching filters. This caught and fixed a real release-tier omission: `CLIProgressTests` was not assigned to any suite group and is now part of `e2e-command-surface`.
  - Progress: suite-health reports now distinguish selected groups from executed groups, record `continue_on_failure`, `live_included`, and `live_skipped`, and mark release-tier live smoke as `skipped` before any early-failure report is written. Focused script coverage proves `--continue-on-failure` runs later selected groups after an earlier failure and records both outcomes.
  - Progress: the suite manifest now enforces single ownership for selected `XCTestCase` classes, so duplicate class filters fail before SwiftPM runs instead of inflating release runtime or making suite-health reports ambiguous. The release coverage check now states that every non-live XCTest class is covered exactly once.
  - Progress: a suite group that exits 0 but reports `Executed 0 tests` now fails as a suite-health error with exit 65 before the report can claim success. This closes the remaining zero-test false-confidence path outside SwiftPM's explicit "No matching test cases were run" warning.
  - Progress: suite group manifest rows are now validated before selection: rows must have exactly four fields, a supported `fast`/`release`/`live` tier, safe group IDs/filter tokens, nonempty descriptions, and unique group IDs. Malformed or duplicate rows fail before SwiftPM runs, keeping release reports tied to an unambiguous manifest.

- [x] Add a checked targeted-test runner that fails when a filter matches zero tests.
  - Severity: S1 false-confidence risk.
  - Evidence: recent targeted verification hit stale filters such as `DoctorCommandTests/testDoctorPassesWithHealthyFakeToolchain` and `DoctorCommandTests/testDoctorReportsCommandLineToolsSelection`; SwiftPM exited 0 with a "No matching test cases were run" warning.
  - Acceptance criteria: provide a repo script or wrapper for filtered Swift tests that fails nonzero when no tests run, update developer/release docs to use it, and keep the hardening-matrix command validation separate from developer targeted-test validation.
  - Progress: `scripts/run-swift-test-filter.sh` now requires at least one `--filter`, preserves SwiftPM stdout/stderr, and exits 64 when SwiftPM reports "No matching test cases were run"; the hardening-matrix runner executes rows through this wrapper, and README/public-alpha docs point targeted verification at it.

- [x] Make live-smoke freshness an explicit release artifact instead of implicit `swift test` coverage.
  - Severity: S1 public-alpha evidence gap.
  - Evidence: `LiveXcodeManagedParallelSmokeTests` skips unless both `XCSTEWARD_RUN_LIVE_XCODE_MANAGED_SMOKE=1` and `XCSTEWARD_LIVE_SIMULATOR_ID` are set, so a full `swift test` pass is still fake-tool-only for live Xcode behavior.
  - Acceptance criteria: require a timestamped live-smoke or live hardening-matrix report inside the release window, distinguish fake full-suite status from live coverage in the public-alpha checklist, and make skipped live tests visible in release notes.
  - Progress: public-alpha docs now require separate fake and live suite-health/hardening-matrix report paths; `scripts/run-test-suite.sh --tier live` records `live_smoke_status`, while release-tier reports mark live coverage as skipped unless `--include-live` is explicitly requested.
  - Progress: live smoke success logs now print `XCSTEWARD_LIVE_SMOKE_EVIDENCE` with the state root, job ID, summary, run metadata, `.xcresult`, test count, and probe-warning count, so the suite-health report points at actionable job evidence.

- [x] Add fixture self-tests for generated fake tool scripts.
  - Severity: S2, promoted to S1 if fixture churn continues.
  - Evidence: generated shell scripts now live across `FakeToolXcodebuildScript.swift` (494 lines), `FakeToolXcrunScript.swift` (346 lines), and `FakeToolUtilityScripts.swift` (127 lines); Swift compilation does not validate shell syntax or unexercised branches.
  - Acceptance criteria: install all fake tools in a fixture test, run shell syntax checks on generated scripts, and smoke-exercise baseline `xcodebuild`, `xcrun`, `ps`, `memory_pressure`, `pmset`, and `xcode-select` commands.
  - Progress: `FakeToolScriptSelfTests` installs the generated scripts, runs `bash -n` over each baseline fake tool, and smoke-exercises baseline commands for `xcodebuild`, `xcrun`, `ps`, `memory_pressure`, `pmset`, and `xcode-select`.

- [x] Split or optimize `DoctorCommandTests` by check group.
  - Severity: S1 maintainability and feedback-loop risk.
  - Evidence: after the Xcode-environment, CoreSimulator, and project-preflight splits, `DoctorCommandTests.swift` was still 384 lines with 14 tests and the combined doctor-preflight group took 159.036s, despite production doctor code already being split into focused health/preflight modules.
  - Acceptance criteria: move doctor tests into environment, CoreSimulator, project preflight, state health, and output/registry groups that match the production modules, and make it cheap to run a single doctor check family without running the whole command suite.
  - Progress: Xcode selection, first-launch, simulator SDK, and CLI-alignment doctor checks moved into `DoctorXcodeEnvironmentCommandTests`.
  - Progress: CoreSimulator runtime, runtime-availability parsing, unavailable-device cleanup, and JSON-health doctor checks moved into `DoctorCoreSimulatorCommandTests`; hardening-matrix rows and the release-tier `doctor-preflight` group now point at the split test classes.
  - Progress: project destination, test-plan, package-resolution, DerivedData, `.xctestrun`, and `xcresulttool` doctor checks moved into `DoctorProjectPreflightCommandTests`; matching hardening-matrix rows and public-alpha targeted examples now point at the split test class.
  - Progress: disk-health, path-safety, state-health, and managed-simulator command checks moved into `DoctorDiskHealthCommandTests`, `DoctorPathSafetyCommandTests`, `DoctorStateHealthCommandTests`, and `DoctorManagedSimulatorCommandTests`; hardening-matrix rows and the release-tier `doctor-preflight` group now point at the focused classes.
  - Resolution: `DoctorCommandTests.swift` is now a small command-output/help surface, while doctor check families can be run independently by production module area.

- [x] Record suite-health evidence during release validation.
  - Severity: S2 release-process gap.
  - Evidence: suite size, skipped live coverage, and slow-suite concentration were discovered manually from terminal output; there is no durable report alongside hardening-matrix and dogfood evidence.
  - Acceptance criteria: release validation records full-suite test count, failure count, skipped count, runtime, slowest suites, and live-smoke status in a timestamped artifact that can be linked from the backlog or dogfood ledger.
  - Progress: `scripts/run-test-suite.sh` writes JSON with status, tier, test/failure/skipped counts, total duration, per-group durations, slowest groups, live smoke status, and durable per-group logs; README and public-alpha docs point release validation at the report path.
  - Validation: `.build/test-suite/public-alpha-live-current.json` records a passed live tier with one test, zero failures, zero skips, `live_smoke_status: passed`, and a group log containing the live XCSteward job evidence line.
