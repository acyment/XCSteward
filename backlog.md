# XCSteward Backlog

## Pending

- [ ] Make `ProcessToolRunner.pollExitStatus` fail safely on unexpected `waitpid` errors.
  - `Sources/XCStewardKit/Support.swift`
  - Return or throw a nonzero/tool failure when `waitpid` returns an error other than `EINTR`; do not let the zero-initialized status map to success.

- [ ] Make queued-job claiming atomic.
  - `Sources/XCStewardKit/StateStore.swift`
  - Replace the `SELECT next queued` + unconditional `UPDATE` sequence with a conditional claim/transaction so future concurrent dispatchers cannot claim the same job.

- [ ] Harden `doctor` `.xctestrun` integrity scratch handling.
  - `Sources/XCStewardKit/Doctor.swift`
  - Use a unique per-run scratch directory or a lock, stop swallowing cleanup failures, and verify `.xctestrun` files were generated by the current build.

- [ ] Scope or gate global unavailable-simulator cleanup in `doctor --fix`.
  - `Sources/XCStewardKit/Doctor.swift`
  - Avoid running global `xcrun simctl delete unavailable` as an implicit XCSteward-owned cleanup on shared Macs; require an explicit global cleanup mode or leave it as a manual action.

- [ ] Keep queued work alive through temporary capacity backpressure.
  - `Sources/XCStewardKit/Worker.swift`
  - Do not exit the worker solely because effective capacity is `0` and no jobs are active while queued jobs remain; distinguish hard drain from temporary host constraints.

- [ ] Preserve first-attempt result bundles before retries.
  - `Sources/XCStewardKit/Executor.swift`
  - `Sources/XCStewardKit/ManualShardRunner.swift`
  - Move retryable partial `.xcresult` bundles to attempt-specific artifact paths before deleting/reusing the canonical result path.

- [ ] Add fatal-error short-circuiting for manual shard runs.
  - `Sources/XCStewardKit/ManualShardRunner.swift`
  - Stop launching or continue-running peer shards when a clear shared setup/config/artifact error makes the whole run unrecoverable.

- [ ] Parse managed simulator discovery from `simctl list devices --json`.
  - `Sources/XCStewardKit/SimulatorLifecycle.swift`
  - Replace text parsing of parenthesized tokens with structured `name`/`udid` matching.

- [ ] Reduce brittle string matching for simulator/Xcode state checks.
  - `Sources/XCStewardKit/SimulatorLifecycle.swift`
  - `Sources/XCStewardKit/Doctor.swift`
  - `Sources/XCStewardKit/ProcessDetection.swift`
  - Prefer JSON/capability probes or normalized parsers over exact output substrings such as `current state: Booted`, `platform:iOS Simulator`, and command-line `contains(" test")`.

- [ ] Surface diagnostic/reporting probe failures instead of silently dropping them.
  - `Sources/XCStewardKit/XCResultReader.swift`
  - `Sources/XCStewardKit/ResultReporting.swift`
  - Record warnings when `xcresulttool`, `xcodebuild -help`, or `xcodebuild -version` fail, time out, or produce unparseable output.

- [ ] Use real elapsed duration for manual-shard JUnit output.
  - `Sources/XCStewardKit/ManualShardRunner.swift`
  - Replace the hard-coded `durationSeconds: 0` with measured manual-run duration or aggregated shard duration.

- [ ] Make host-capacity sampling defaults explicit.
  - `Sources/XCStewardKit/HostCapacity.swift`
  - Decide whether memory/thermal sampling should be enabled by default or documented as opt-in, and expose the resulting capability clearly in host health/status.

- [ ] Add protected-path validation beyond `stateRoot`.
  - `Sources/XCStewardKit/Doctor.swift`
  - Check project paths, repo roots, derived-data overrides, and other writable configured paths against protected/shared locations, not only the state root.

- [ ] Split large architecture hotspots after behavior hardening.
  - `Sources/XCStewardKit/Doctor.swift`
  - `Tests/XCStewardKitTests/EndToEndCommandTests.swift`
  - `Tests/XCStewardKitTests/FakeToolFixtures.swift`
  - Break doctor checks into domain files and split oversized E2E/fake-tool fixtures to reduce change risk.
